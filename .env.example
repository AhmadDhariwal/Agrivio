# Agrivio local environment example
# Copy to .env.local for developer overrides. Do not commit secrets.
# Exact application variable names are finalized in later F00/F01 work items.

NODE_ENV=development
