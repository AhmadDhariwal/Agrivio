# R1-F00-003 — Express Backend Scaffold

> **Historical paths:** Completed as TypeScript `apps/api` / Nx `api`. Later renamed to JavaScript ESM `apps/backend` / Nx `backend` per [F00-APP-NAMING-BACKEND-JS-MIGRATION.md](F00-APP-NAMING-BACKEND-JS-MIGRATION.md). **Current canonical backend module format is CommonJS** per [BACKEND-COMMONJS-MIGRATION.md](BACKEND-COMMONJS-MIGRATION.md). ESM references below are historical evidence.

## Task Status

* Status: Complete
* Result: Empty Express `apps/api` scaffold builds, lints, type-checks, runs Vitest boot smoke, and executes as native ESM
* Completion date: 2026-08-05
* Commit: e9d93bf8a4149708d81324cd519f880c8c5a7c18
* Next work item: R1-F00-004 — Shared Tooling and API Contracts Packages

## Goal

Generate only `apps/api` as an executable empty Express/TypeScript API using the frozen Node/Express configuration, without business routes, auth, schemas, shared packages, CI, or MongoDB configuration.

## Frozen Sources

* [TOOLCHAIN.md](../TOOLCHAIN.md) v1.0.1
* [REPOSITORY_INITIALIZATION.md](../REPOSITORY_INITIALIZATION.md) v1.0.1 §6 / §5a
* [TEST_STRATEGY.md](../TEST_STRATEGY.md) v1.0.1
* [DEVELOPMENT_WORKFLOW.md](../DEVELOPMENT_WORKFLOW.md) v1.0.1
* [IMPLEMENTATION_ROADMAP.md](../IMPLEMENTATION_ROADMAP.md) `R1-F00-003`
* [QUALITY_GATES.md](../QUALITY_GATES.md)
* [AGENTS.md](../../AGENTS.md)

## Scope Completed

* Generated `apps/api` with Express `5.2.1`, TypeScript `6.0.3`, native ESM (`"type": "module"`), ES2024, NodeNext, strict TypeScript, esbuild, Vitest, ESLint
* Minimum executable scaffold: `createApp()` + listen entrypoint; no business routes
* API boot smoke test via Vitest (listen on ephemeral port, assert 404 without routes)
* Direct dependencies pinned to exact TOOLCHAIN versions
* Mongoose deferred (not required by this work item; no persistence wiring)
* Existing Agrivio root, Nx workspace, and `apps/web` preserved

## Dry-Run Gate Evidence

1. `pnpm exec nx g @nx/node:application --help` confirmed frozen options:
   * `--framework=express`
   * `--bundler=esbuild`
   * `--e2eTestRunner=none`
   * `--linter=eslint`
   * `--name`
2. Schema limitation: `--unitTestRunner` choices are only `jest` and `none` (not `vitest`).
3. Approved command with `--unitTestRunner=vitest --dry-run` failed schema validation: `'vitest' should be one of jest,none`.
4. Did not substitute Jest. Generated with `--unitTestRunner=none`, then configured Vitest via `@nx/vitest:configuration` (`--testEnvironment=node`, `--uiFramework=none`, `--coverageProvider=v8`).
5. Dry-run with `--unitTestRunner=none` succeeded and targeted path `apps/api` (NOTE: dry-run reported no changes applied).

## Generation Commands

```bash
pnpm exec nx g @nx/node:application apps/api \
  --name=api \
  --framework=express \
  --bundler=esbuild \
  --unitTestRunner=none \
  --e2eTestRunner=none \
  --linter=eslint

pnpm exec nx g @nx/vitest:configuration \
  --project=api \
  --testEnvironment=node \
  --uiFramework=none \
  --coverageProvider=v8 \
  --runtimeTsconfigFileName=tsconfig.app.json
```

## Out of Scope (Confirmed Absent)

* Business routes (`app.get` / routers)
* Final health endpoint
* Authentication
* Mongoose schemas/models (Mongoose not installed)
* `apps/api/src/modules/` placeholder folders
* Generic CRUD / repository frameworks
* Shared `packages/` projects
* MongoDB / Docker configuration
* CI workflows
* Nested workspace

## Files Created or Updated

| Path | Action |
| --- | --- |
| `apps/api/**` | Created (minimal Express ESM scaffold + boot smoke) |
| `apps/api/package.json` | Created (`@agrivio/api`, `"type": "module"`) |
| `package.json` | Updated with Express/types/esbuild exact pins; `@nx/vite`/`@nx/esbuild` |
| `pnpm-lock.yaml` | Updated |
| `nx.json` | Updated by generators |
| `eslint.config.mjs` | Updated by Vitest generator |
| `.gitignore` | Updated by Vitest generator |
| `vitest.workspace.ts` | Created by Vitest generator |
| `.vscode/launch.json` | Created by Node generator (gitignored via `.vscode/*`) |
| `docs/tasks/R1-F00-003.md` | Created |
| `README.md` | Updated |
| `docs/PROJECT_INDEX.md` | Updated |

## Exact Versions Observed

| Package / setting | Version / value |
| --- | --- |
| Node.js | `24.18.0` |
| pnpm | `11.17.0` |
| Express | `5.2.1` |
| TypeScript | `6.0.3` |
| Vitest | `4.1.10` |
| ESLint | `10.7.0` |
| `@types/express` | `5.0.6` |
| `@types/node` | `24.13.3` |
| esbuild | `0.27.7` |
| `@nx/node` / `@nx/express` / `@nx/esbuild` / `@nx/vite` / `@nx/vitest` | `23.1.0` |
| Mongoose | not installed |
| API `"type"` | `module` |
| API `target` / `module` / `moduleResolution` | `ES2024` / `NodeNext` / `NodeNext` |
| Build format | `esm` |

## Commands Run

```text
pnpm exec nx g @nx/node:application --help
pnpm exec nx g @nx/node:application apps/api ... --unitTestRunner=vitest --dry-run
pnpm exec nx g @nx/node:application apps/api ... --unitTestRunner=none --dry-run
pnpm exec nx g @nx/node:application apps/api ... --unitTestRunner=none
pnpm exec nx g @nx/vitest:configuration --project=api ... --dry-run
pnpm exec nx g @nx/vitest:configuration --project=api ...
pnpm install
pnpm install --frozen-lockfile
pnpm exec nx build api
pnpm exec nx run api:eslint:lint
pnpm exec tsc -b apps/api/tsconfig.json
pnpm exec nx test api
node dist/apps/api/main.js  # native ESM listen smoke
pnpm exec nx build web
```

## Validation Results

| Check | Result |
| --- | --- |
| Express exact `5.2.1` | Pass |
| Native ESM `"type": "module"` | Pass |
| ES2024 / NodeNext / strict TypeScript | Pass |
| `nx build api` | Pass |
| `nx run api:eslint:lint` | Pass |
| `tsc -b apps/api/tsconfig.json` | Pass |
| `nx test api` (Vitest boot smoke, 1 file / 1 test) | Pass |
| Frozen lockfile install | Pass |
| Native ESM execution (`node dist/apps/api/main.js`) | Pass |
| No business routes / modules / auth / mongoose | Pass |
| No `packages/` / CI / MongoDB | Pass |
| `apps/web` still builds | Pass |

## Quality Gates

| Gate | Result |
| --- | --- |
| WI-G01 Scope check | Pass |
| WI-G02 Frozen-document traceability | Pass |
| WI-G03 Type checking | Pass |
| WI-G04 Linting | Pass |
| WI-G05 Unit tests | Pass — API boot smoke |
| WI-G06–WI-G13 | N/A — no persistence/tenant/auth/subscription behaviour |
| WI-G14 Documentation update | Pass |
| WI-G15 Diff review | Pass |

## Definition of Done

Empty API builds, boots in test mode, lints, and type-checks without empty future-module placeholder folders.

## Remaining Risks

* `@nx/node:application@23.1.0` does not accept `--unitTestRunner=vitest`; Vitest was wired via `@nx/vitest:configuration` instead of Jest.
* Generator emits deprecated `@nx/vitest:test` / `nxViteTsPaths` warnings for Nx v24 migration later.
* Root command contract scripts (`pnpm build:api`, etc.) remain for `R1-F00-005`.
* Architecture-boundary fixture remains for `R1-F00-008`.

## Completion Report

### Summary

R1-F00-003 complete. `apps/api` is an empty Express 5.2.1 native-ESM TypeScript application with Vitest boot smoke. Exact TOOLCHAIN pins are applied; Mongoose and business routes are absent.

### Files Changed

See Files Created or Updated above.

### Validation Performed

Help + dry-run evidence, Vitest unitTestRunner schema workaround, exact version pinning, build, lint, TypeScript project build, Vitest boot smoke, frozen-lockfile install, native ESM execution, absence checks, and `apps/web` build compatibility.

### Documentation Updated

* `docs/tasks/R1-F00-003.md`
* `README.md`
* `docs/PROJECT_INDEX.md`

### Remaining Risks or Blockers

No blockers. Risks listed above.

### Confirmation

No unrelated frozen-document edits. No shared packages, CI, MongoDB, authentication, Mongoose, or business module folders were created.
