# P1-05 — Data, API, Security, and Subscription Design

## Task Status

* Status: Complete
* Result: Data, API, security, authorization, subscription, and billing design approved and frozen
* Completion date: 2026-08-04
* Technical-design version: 1.0
* Canonical collections: 44
* API endpoints: 157
* Idempotent endpoints: 38
* Permissions: 81
* Canonical modules covered: 20
* Next task: P1-06 — Implementation Roadmap and Delivery Plan

## Goal

Define the production-grade Release 1 data model, MongoDB collection ownership, indexes, transaction and concurrency rules, API conventions, authentication and session strategy, authorization model, permission catalog, subscription lifecycle, entitlement enforcement, and manual billing workflow for Agrivio.

This task converts the frozen product, business-rule, and architecture documents into implementation-ready technical design.

This task must create:

* `docs/DATA_MODEL.md`
* `docs/API_DESIGN.md`
* `docs/SECURITY_AUTHORIZATION.md`
* `docs/SUBSCRIPTION_AND_BILLING.md`

This task must update:

* `docs/PROJECT_INDEX.md`
* `README.md`

This task defines technical design only.

Do not initialize applications, install packages, create schemas, generate Mongoose models, create routes, create source folders, or write application code.

---

# 1. Files to Read

Read only:

* `AGENTS.md`
* `README.md`
* `docs/PROJECT_INDEX.md`
* `docs/PROJECT_DECISIONS.md`
* `docs/PRD.md`
* `docs/RELEASE_1_SCOPE.md`
* `docs/BUSINESS_RULES.md`
* `docs/DOMAIN_GLOSSARY.md`
* `docs/ARCHITECTURE.md`
* `docs/MODULE_BOUNDARIES.md`
* `docs/REPOSITORY_STRUCTURE.md`
* `docs/tasks/P1-05.md`

Do not scan unrelated files.

---

# 2. Authority Order

Use this authority order:

1. `docs/PROJECT_DECISIONS.md`
2. `docs/PRD.md`
3. `docs/RELEASE_1_SCOPE.md`
4. `docs/BUSINESS_RULES.md`
5. `docs/DOMAIN_GLOSSARY.md`
6. `docs/ARCHITECTURE.md`
7. `docs/MODULE_BOUNDARIES.md`
8. `docs/REPOSITORY_STRUCTURE.md`
9. `docs/tasks/P1-05.md`
10. `AGENTS.md`

P1-02, P1-03, and P1-04 documents are frozen.

P1-05 must not:

* Change product scope
* Change business formulas
* Change domain terminology
* Change module ownership
* Change dependency directions
* Introduce client-specific code forks
* Introduce microservices
* Begin implementation

Report genuine conflicts as blockers instead of silently changing frozen documents.

---

# 3. Deliverables

## 3.1 `docs/DATA_MODEL.md`

Define:

* MongoDB modeling principles
* Canonical collection catalog
* Collection ownership by module
* Embedded versus referenced data rules
* Tenant ownership rules
* Platform-owned versus tenant-owned data
* Common technical fields
* Money and quantity representation
* Time and business-date representation
* Immutable transaction snapshots
* Master-data lifecycle
* Posted-record lifecycle
* Signed financial effects
* Movement-derived balances
* Collection relationships
* Required indexes
* Unique constraints
* Partial indexes
* TTL indexes
* Transaction boundaries
* Concurrency controls
* Idempotency persistence
* Reconciliation strategy
* Retention and deletion rules
* Migration and import implications
* Controlled unresolved data details

## 3.2 `docs/API_DESIGN.md`

Define:

* API style
* Versioning
* URL conventions
* Request and response envelopes
* Error envelope
* Authentication transport
* Tenant-context handling
* Pagination
* Filtering
* Sorting
* Date and decimal serialization
* Idempotency
* Optimistic concurrency
* Draft/post/cancel/reverse action patterns
* Master-data mutation patterns
* Transaction endpoint rules
* Import endpoint workflow
* Reporting and export patterns
* Platform API separation
* API permission mapping
* Endpoint inventory by module
* API contract testing expectations

## 3.3 `docs/SECURITY_AUTHORIZATION.md`

Define:

* Authentication strategy
* Session design
* Password policy
* Login and reset behaviour
* Session expiration and invalidation
* CSRF protection
* CORS policy
* Rate limiting
* Security headers
* Input validation boundaries
* Tenant isolation enforcement
* Branch and warehouse scoping
* Permission naming
* Role-to-permission defaults
* Approval versus permission
* Subscription entitlement enforcement
* Audit requirements
* Sensitive-data handling
* Logging restrictions
* Backup/restore authorization
* Security testing expectations
* Controlled unresolved security details

## 3.4 `docs/SUBSCRIPTION_AND_BILLING.md`

Define:

* Plan model
* Plan entitlements
* Configurable limits
* Trial behaviour
* Billing periods
* Manual payment evidence
* Approval and rejection
* Subscription states
* Grace behaviour
* Suspension
* Reactivation
* Cancellation
* Retention
* Upgrade and downgrade behaviour
* Limit enforcement
* Dedicated-cloud entitlement
* Audit requirements
* Commercially unresolved details

---

# 4. Document Control

Each document must contain:

```text
Document status: Draft for P1-05 review
Current version: 0.1
Approval status: Not yet frozen
```

Use the actual update date.

State:

* Frozen requirements define product scope.
* Frozen Business Rules define calculations and behaviour.
* Frozen architecture defines module ownership and dependencies.
* P1-05 defines implementation-ready technical contracts.
* P1-05 does not create implementation.

Do not freeze documents automatically.

---

# 5. MongoDB Modeling Principles

`DATA_MODEL.md` must preserve these principles.

## 5.1 Database baseline

* MongoDB is the Release 1 persistence database.
* Mongoose will be the persistence mapping layer.
* Exact Mongoose schema code is not created in P1-05.
* Collection ownership follows `MODULE_BOUNDARIES.md`.
* One module must not access another module’s collection directly.
* Cross-module access occurs through public module interfaces.

## 5.2 Identifiers

* MongoDB `ObjectId` is the internal record identifier.
* API contracts expose identifiers as opaque strings.
* Security must not depend on identifiers being difficult to guess.
* Every identifier lookup must still enforce organization ownership and authorization.
* Human-readable invoice numbers remain separate from internal identifiers.

## 5.3 Common fields

Tenant-owned records must include, where applicable:

```text
_id
organizationId
createdAt
updatedAt
createdBy
updatedBy
version
status
```

Posted or corrective records may additionally include:

```text
postedAt
postedBy
sourceType
sourceId
correctionOfId
reversalOfId
reason
```

Rules:

* `organizationId` is mandatory for tenant-owned data.
* Tenant-owned repositories must never treat `organizationId` as an optional filter.
* `version` supports optimistic concurrency on mutable records.
* Immutable posted records must not be updated through normal mutation APIs.
* Audit information must not be silently replaced during updates.

## 5.4 Platform-owned records

Platform-owned records may omit `organizationId` only when they are genuinely platform scoped.

Examples:

* Global identity
* Platform subscription-plan definitions
* Platform operation status
* Cross-organization Super Admin operations

A record must not be treated as platform owned merely to avoid tenant filtering.

---

# 6. Decimal and Date Representation

Define:

## Money

* MongoDB stores monetary values using Decimal128-compatible decimal representation.
* API contracts serialize monetary values as decimal strings.
* Monetary values use two decimal places in final posted values.
* JavaScript binary floating-point must not be authoritative for business calculations.
* Currency remains PKR in Release 1.

Example API value:

```json
{
  "amount": "1250.50",
  "currency": "PKR"
}
```

## Quantity

* MongoDB stores quantities using Decimal128-compatible decimal representation.
* API contracts serialize quantities as decimal strings.
* Final posted base quantities support four decimal places.
* Conversion factors support six decimal places.

## Time

* Store timestamps in UTC.
* Serialize timestamps using ISO 8601 UTC format.
* Store organization business timezone separately.
* Business-date rules use organization timezone.
* Date-only business values such as expiry dates must remain date-only concepts and must not shift because of UTC conversion.

---

# 7. Canonical Collection Catalog

`DATA_MODEL.md` must define the following canonical collection names and ownership.

## Identity and Access

```text
users
organization_memberships
auth_sessions
password_reset_tokens
```

Rules:

* `users` owns global authentication identity.
* Normalized login email is globally unique.
* `organization_memberships` links a user to an organization, role bundle, status, and access assignments.
* A user may hold platform access or organization membership according to authorization rules.
* Session tokens and reset tokens are never stored in plaintext.

## Organizations

```text
organizations
organization_settings
```

## Subscriptions

```text
subscription_plans
subscriptions
subscription_billing_records
```

## Locations

```text
branches
warehouses
access_assignments
invoice_sequences
```

## Catalog and Pricing

```text
product_categories
products
product_packaging_units
product_prices
```

## Customers and Suppliers

```text
customers
suppliers
```

## Inventory

```text
product_batches
stock_movements
inventory_balances
inventory_cost_states
warehouse_transfers
stock_adjustments
```

## Purchases

```text
purchases
```

Purchase lines remain embedded immutable snapshots inside purchase records unless a later measured document-size constraint requires an ADR-approved change.

## Sales

```text
sales
```

Sale lines remain embedded immutable snapshots inside sale records.

## Payments and Ledgers

```text
payments
payment_allocations
ledger_effects
```

## Accounts and Expenses

```text
accounts
account_movements
expense_categories
expenses
```

## Returns and Corrections

```text
returns
corrective_transactions
```

Return lines remain embedded snapshots inside return records.

## Alerts

```text
notification_items
```

`notification_items` may store presentation or acknowledgement state only.

It must not become the source of truth for stock, expiry, receivables, or payables.

## Reporting

Reporting owns no authoritative transactional collection.

Read models or cached projections require a later approved design and must remain rebuildable from authoritative data.

## Imports

```text
import_jobs
import_row_errors
```

## Audit

```text
audit_events
```

## Operations

```text
backup_operation_records
restore_operation_records
```

Operations records must not contain normal business transactions.

---

# 8. Embedded and Referenced Data

Define:

## Embed when

* The data is an immutable transaction snapshot.
* It is always read with its owning transaction.
* It must preserve historical values.
* It does not have an independent lifecycle.

Examples:

* Sale lines
* Purchase lines
* Return lines
* Unit and conversion snapshots
* Price and cost snapshots
* Payment-method breakdown inside a payment command where appropriate

## Reference when

* The record has its own lifecycle.
* It is queried independently.
* It may grow without a safe bounded size.
* Multiple modules need access through the owning module’s public interface.

Examples:

* Customers
* Suppliers
* Products
* Warehouses
* Payments
* Ledger effects
* Stock movements
* Account movements
* Audit events

Posted snapshots retain both the reference identifier and required historical display values.

Changing master data must not alter historical transactions.

---

# 9. Authoritative and Projected State

Define:

## Authoritative movement/effect records

* Stock movements
* Signed ledger effects
* Account movements
* Payment allocations
* Posted sales
* Posted purchases
* Returns
* Corrective transactions

## Maintained projections

* `inventory_balances`
* `inventory_cost_states`
* Derived current customer balance
* Derived current supplier balance
* Derived current account balance
* Alert presentation state

Rules:

* A projection must reconcile to authoritative effects.
* Projections may improve read performance.
* Projections must not conflict with authoritative movement history.
* Reconciliation tests must be able to rebuild or verify projected totals.
* Reporting must not create an independent conflicting calculation.

---

# 10. Posted Transaction Design

Sales, purchases, returns, payments, transfers, adjustments, expenses, and corrective transactions must distinguish:

```text
draft
posted
cancelled or reversed where applicable
```

Rules:

* Drafts may be edited or discarded.
* Posted records are immutable.
* Posted records are never permanently deleted.
* Cancellation and reversal create linked corrective records.
* The original record remains preserved.
* A transaction cannot be corrected twice through the same correction path.
* Corrective records use signed effects opposite to their source.
* Posted line snapshots preserve product, unit, conversion, price, cost, batch, customer, supplier, and relevant display facts.

---

# 11. Inventory Data Rules

Define:

* `stock_movements` is the authoritative quantity history.
* `inventory_balances` is the maintained current projection.
* `inventory_cost_states` stores current warehouse-product weighted-average cost state.
* `product_batches` defines batch identity.
* Warehouse-specific quantity remains in balances and movements, not duplicated as batch identity.
* Loose stock from separate batches remains separate.
* Transfer outbound and inbound movements link to one transfer.
* Failed transfers create neither movement.
* Negative stock override is recorded on the source transaction and audit event.
* Later receipts do not recalculate historical sale COGS.

Required scopes:

```text
organizationId
warehouseId
productId
batchId where applicable
```

---

# 12. Signed Effect Design

`ledger_effects` and `account_movements` must use explicit signed effect direction.

Do not store ambiguous generic “reversal amounts.”

Examples:

* Credit sale receivable: positive customer ledger effect
* Customer payment allocation: negative customer ledger effect
* Reversal of customer payment: positive customer ledger effect
* Purchase payable: positive supplier ledger effect
* Supplier payment: negative supplier ledger effect
* Account receipt: positive account movement
* Account payment: negative account movement

Rules:

* Every corrective effect references its source effect.
* Source plus complete reversal must net to zero.
* Original and reversal must not be double-subtracted.
* Balance calculation uses the sum of signed posted effects.

---

# 13. Required Indexes

Define indexes at design level.

## Common tenant indexes

Every tenant-owned collection requires an organization-leading index appropriate to its primary queries.

Examples:

```text
{ organizationId: 1, createdAt: -1 }
{ organizationId: 1, status: 1, createdAt: -1 }
```

## Identity

* Unique normalized email on `users`
* Unique `{ organizationId, userId }` on memberships
* TTL expiry on sessions
* TTL expiry on password-reset tokens
* Hashed session-token lookup
* Hashed reset-token lookup

## Locations

* Unique branch invoice sequence ownership
* Unique posted invoice number:

```text
{ organizationId: 1, branchId: 1, invoiceNumber: 1 }
```

Use a partial unique index where required so drafts without invoice numbers do not conflict.

## Catalog

* Organization-scoped product search indexes
* Partial unique organization-scoped SKU when SKU is present
* Organization-scoped normalized category-name lookup
* Organization/product/price-tier lookup

Do not make optional fields globally unique.

## Customers and Suppliers

* Organization-scoped normalized name and phone search
* Organization-scoped customer/supplier status indexes
* Supplier-reference duplicate-warning lookup remains non-unique

## Inventory

* Organization/warehouse/product/batch movement lookup
* Organization/product/batch-number identity lookup
* Organization/warehouse/product current-balance lookup
* Organization/warehouse/product cost-state lookup
* Expiry-date indexes for tracked batches
* Posted-at ordering indexes for FIFO and FEFO queries

## Transactions

* Organization/status/date indexes
* Organization/branch/date sales indexes
* Organization/warehouse/date purchase and sales indexes
* Source transaction and correction-reference indexes
* Idempotency-key unique indexes where applicable

## Ledgers and Accounts

* Organization/customer/date ledger indexes
* Organization/supplier/date ledger indexes
* Organization/account/date movement indexes
* Source and allocation reference indexes

## Audit

* Organization/date
* Organization/actor/date
* Organization/sourceType/sourceId
* Platform operation/date where applicable

Exact index names are not required, but field order and purpose must be documented.

---

# 14. Transaction Boundaries

Use MongoDB transactions for frozen atomic workflows.

The orchestrating application service owns the transaction boundary.

Required transactional workflows:

* Sale posting
* Purchase posting
* Return posting
* Warehouse transfer
* Customer payment
* Supplier payment
* Account transfer
* Expense posting
* Opening balances
* Opening stock
* Cancellation
* Reversal
* Import execution

Rules:

* Participating module methods accept shared transaction context.
* Participating modules must not independently commit.
* Audit events required for correctness participate in the same transaction.
* A failed workflow rolls back all authoritative effects.
* After-commit events cannot establish authoritative stock or financial state.
* Transaction retry must not duplicate business effects.

---

# 15. Concurrency and Idempotency

Define:

## Optimistic concurrency

Mutable master data uses a version field.

An update must provide the expected version.

A stale update returns a conflict rather than overwriting newer data.

## Invoice sequencing

* Sequence allocation is atomic.
* Posted invoice numbers are unique by organization and branch.
* Cancelled numbers are not reused.
* Sequence gaps are allowed.
* A failed retry must not create a duplicate invoice.

## Stock and cost

* Stock validation and movement posting occur in the same transaction.
* WAC state updates use transaction-safe conditional updates.
* Concurrent sale or purchase posting must not silently overwrite stock or cost state.

## Idempotency

Require an `Idempotency-Key` for:

* Sale posting
* Purchase posting
* Return posting
* Payment posting
* Transfer posting
* Cancellation
* Reversal
* Import execution

Idempotency scope must include:

```text
organization
authenticated actor
operation
idempotency key
```

Repeated requests with the same key and equivalent payload return the original result.

Reuse with a conflicting payload returns a conflict.

Idempotency records must have a controlled retention period defined before implementation.

---

# 16. API Baseline

`API_DESIGN.md` must define:

* JSON over authenticated HTTP
* Base prefix:

```text
/api/v1
```

* UTF-8
* Plural kebab-case resource names
* Opaque string identifiers
* ISO 8601 UTC timestamps
* Decimal strings for money, quantities, and conversion factors
* No organization ID accepted as authoritative tenant context for normal organization users
* Platform APIs separated under:

```text
/api/v1/platform
```

* Normal organization APIs infer organization context from authenticated membership.

P1-05 does not create Express routes.

---

# 17. Response Envelope

Successful response:

```json
{
  "data": {},
  "meta": {},
  "requestId": "opaque-request-id"
}
```

Rules:

* `meta` is optional.
* `requestId` is always returned.
* Empty successful operations may return `data: null`.

List response:

```json
{
  "data": [],
  "meta": {
    "page": 1,
    "pageSize": 25,
    "total": 100
  },
  "requestId": "opaque-request-id"
}
```

Error response:

```json
{
  "error": {
    "code": "BUSINESS_RULE_VIOLATION",
    "message": "Human-readable safe message",
    "details": []
  },
  "requestId": "opaque-request-id"
}
```

Do not expose stack traces, database errors, collection names, or secrets.

---

# 18. Error Codes

Define stable categories:

```text
AUTHENTICATION_REQUIRED
INVALID_CREDENTIALS
SESSION_EXPIRED
AUTHORIZATION_DENIED
TENANT_SCOPE_VIOLATION
BRANCH_SCOPE_VIOLATION
WAREHOUSE_SCOPE_VIOLATION
SUBSCRIPTION_RESTRICTED
VALIDATION_FAILED
BUSINESS_RULE_VIOLATION
RESOURCE_NOT_FOUND
VERSION_CONFLICT
DUPLICATE_CONFLICT
IDEMPOTENCY_CONFLICT
TRANSACTION_FAILED
RATE_LIMITED
INTERNAL_ERROR
```

Module-specific error codes may extend these categories later.

HTTP status and internal error code must remain distinct.

---

# 19. Pagination, Filtering, and Sorting

Default list pagination:

```text
page: 1
pageSize: 25
```

Maximum page size:

```text
100
```

Rules:

* Pagination is one-based.
* Every paginated list uses a stable deterministic sort.
* Default transactional sort is newest first.
* Supported filters must be explicitly documented per endpoint.
* Unknown filters are rejected.
* Sort fields use an allowlist.
* Search must remain organization scoped.
* Large-report streaming or asynchronous generation remains a later performance decision.

---

# 20. Mutation Patterns

## Master data

Use:

```text
POST   /resource
GET    /resource
GET    /resource/:id
PATCH  /resource/:id
```

Normal hard deletion is not required.

Use archive/deactivate operations where business history requires references to remain valid.

## Draft transactions

Use patterns such as:

```text
POST   /sales
PATCH  /sales/:id
GET    /sales/:id
POST   /sales/:id/post
POST   /sales/:id/cancel
```

Equivalent patterns apply to purchases and appropriate transactions.

Rules:

* Action endpoints represent business transitions.
* Posted records do not use normal PATCH.
* Posted sales and purchases do not use DELETE.
* Reversal and correction endpoints require reason and authorization.
* Transaction action requests require idempotency.

Exact complete endpoint paths must be documented in the endpoint inventory.

---

# 21. Endpoint Inventory

`API_DESIGN.md` must provide an implementation-ready endpoint inventory grouped by the 20 canonical modules.

Each row must include:

| Method | Path | Purpose | Required permission | Tenant scope | Idempotency | Transaction owner |
| ------ | ---- | ------- | ------------------- | ------------ | ----------- | ----------------- |

Cover at minimum:

* Authentication
* Sessions
* Password reset
* Platform organization approval
* Organizations
* Users and access assignments
* Subscriptions and billing evidence
* Branches
* Warehouses
* Catalog
* Customers
* Suppliers
* Inventory queries
* Opening stock
* Adjustments
* Transfers
* Purchases
* Sales
* Customer payments
* Supplier payments
* Accounts
* Expenses
* Returns
* Alerts
* Dashboard
* Reports
* Imports
* Audit
* Settings
* Backup/restore status

Do not define Mongoose implementation or controller names.

---

# 22. Import API Workflow

Document this API lifecycle:

```text
Create import job
→ upload/provide workbook
→ validate
→ retrieve preview and row errors
→ explicitly confirm
→ execute with idempotency
→ retrieve final result
```

Rules:

* Preview has no business posting effects.
* Execute invokes target-module services.
* Invalid rows are not silently skipped.
* Execution is logically all-or-nothing.
* Import APIs remain tenant scoped.
* Exact spreadsheet fields remain outside P1-05.

---

# 23. Authentication Strategy

Use server-managed opaque sessions for the Release 1 browser application.

## Session transport

* Opaque session token
* Secure cookie
* `HttpOnly`
* `Secure` in production
* `SameSite=Lax`
* Session token stored hashed in the database
* Session identifier rotated after login and sensitive privilege changes
* Frontend JavaScript must not read the session token

Do not use local-storage authentication tokens.

## Session defaults

* Inactivity timeout: 30 minutes
* Absolute session lifetime: 12 hours
* No “remember me” session in Release 1
* Password reset invalidates all existing sessions
* User deactivation invalidates all sessions
* Permission or membership changes invalidate affected sessions
* Organization suspension restricts session capabilities immediately

Session values must remain configurable through secure server configuration, but these are the Release 1 defaults.

---

# 24. Password and Reset Policy

Password policy:

* Minimum 12 characters
* Maximum 128 characters
* Allow passphrases
* Do not require arbitrary uppercase/lowercase/symbol composition rules
* Reject known-common or compromised passwords where supported
* Store passwords using Argon2id
* Never log passwords
* Never return password hashes

Password reset:

* Single-use random token
* Store only token hash
* Expiry: 30 minutes
* Successful reset invalidates all sessions
* Reset responses must not reveal whether an email exists
* Reset actions are rate limited and audited where appropriate

Two-factor authentication is not included in Release 1.

A future 2FA introduction requires a controlled scope/security decision.

---

# 25. CSRF, CORS, and Security Headers

Because Release 1 uses cookie sessions:

* State-changing requests require CSRF protection.
* CSRF tokens must be bound to the authenticated session.
* CORS uses an explicit allowlist.
* Credentials are not permitted from arbitrary origins.
* Production must enforce HTTPS.

Security headers must include an approved baseline for:

* Content Security Policy
* Frame protection
* Content-type sniffing protection
* Referrer policy
* Permissions policy
* HSTS in production

Exact header values may be finalized during implementation hardening without weakening these requirements.

---

# 26. Rate Limiting

Apply stricter rate limits to:

* Login
* Password reset
* Activation
* Billing-evidence submission
* Import upload
* Export generation
* Platform approval actions

Use progressive throttling for repeated authentication failures.

Do not permanently lock legitimate users through an unaudited automatic rule.

Exact numeric limits remain implementation configuration, but security tests must verify throttling exists.

---

# 27. Authorization Model

Authorization consists of separate checks:

```text
authentication
organization scope
subscription entitlement
permission
branch assignment
warehouse assignment
business approval
```

Rules:

* Role names provide default permission bundles.
* Backend authorization uses permission codes.
* Direct role-name checks are prohibited except documented Super Admin platform boundaries.
* Frontend permission checks are usability only.
* Every protected endpoint maps to a permission.
* Every branch or warehouse endpoint validates assignment.
* Business approval does not replace permission.
* Permission does not bypass credit, stock, expiry, or other business rules.

---

# 28. Permission Naming

Use:

```text
<domain>.<action>
```

Examples:

```text
sales.view
sales.create
sales.post
sales.cancel
sales.expired-stock.approve
sales.credit-limit.approve
inventory.view
inventory.adjust
inventory.transfer
inventory.negative-stock.override
```

Permission identifiers are stable public authorization contracts.

Do not use route names or controller names as permissions.

---

# 29. Required Permission Catalog

`SECURITY_AUTHORIZATION.md` must define at least the following canonical permissions.

## Platform

```text
platform.organizations.view
platform.organizations.create
platform.organizations.approve
platform.organizations.suspend
platform.subscriptions.manage
platform.billing.verify
platform.audit.view
operations.backups.view
operations.restore.execute
```

## Organization and users

```text
organization.view
organization.update
users.view
users.create
users.update
users.deactivate
users.assign-access
branches.view
branches.manage
warehouses.view
warehouses.manage
settings.view
settings.manage
```

## Subscription

```text
subscription.view
subscription.billing-evidence.submit
```

## Catalog

```text
catalog.view
catalog.manage
pricing.view
pricing.manage
pricing.override
```

## Customers and suppliers

```text
customers.view
customers.manage
customers.credit-policy.manage
suppliers.view
suppliers.manage
```

## Inventory

```text
inventory.view
inventory.opening-stock.post
inventory.adjust
inventory.transfer
inventory.negative-stock.override
inventory.expiry.view
```

## Purchases

```text
purchases.view
purchases.create
purchases.post
purchases.cancel
purchases.return
```

## Sales

```text
sales.view
sales.create
sales.post
sales.cancel
sales.expired-stock.approve
sales.credit-limit.approve
```

## Payments and accounts

```text
customer-payments.view
customer-payments.post
supplier-payments.view
supplier-payments.post
payments.correct
accounts.view
accounts.manage
accounts.transfer
expenses.view
expenses.post
expenses.correct
```

## Returns

```text
returns.view
returns.post
returns.without-invoice.approve
```

## Reports and alerts

```text
alerts.view
dashboard.view
reports.view
reports.export
```

## Imports and audit

```text
imports.preview
imports.execute
audit.view
```

The agent may add required permissions but must not silently remove these.

---

# 30. Default Role Bundles

Define default role bundles.

## Super Admin

Platform-scoped permissions only, including:

* Organization approval
* Platform subscription management
* Billing verification
* Platform audit
* Backup status
* Controlled restore authorization where operationally assigned

Super Admin does not automatically act as an organization user.

## Owner

Default organization-wide access to all organization permissions, including:

* Users and access
* Branches and warehouses
* Catalog
* Customers and suppliers
* Inventory
* Sales and purchases
* Payments
* Accounts and expenses
* Returns
* Reports
* Imports
* Audit
* Settings
* Negative-stock override
* Required business approvals

Owner has no platform permissions.

## Manager

Operational management permissions, including:

* Catalog, customers, and suppliers
* Inventory and transfers
* Purchases and sales
* Payments and accounts
* Expenses
* Returns
* Reports and exports
* Expired-sale approval
* Credit-limit approval
* Return-without-invoice approval

Manager must not:

* Manage Owners
* Approve organizations
* Manage platform subscriptions
* Execute production restore
* Use negative-stock override unless separately granted

## Cashier

Default access to:

* View catalog and stock availability
* Create and post sales
* Create customers where permitted
* Post customer payments
* View limited customer ledger information
* Print invoices and receipts
* View dashboard information required for POS work

Cashier must not by default:

* Cancel posted sales
* Approve expired sales
* Approve credit-limit overrides
* Post purchases
* Adjust stock
* Transfer stock
* Manage supplier payments
* Manage accounts
* View full audit history

## Store Keeper

Default access to:

* Catalog and inventory view
* Opening stock where specifically assigned
* Purchases
* Purchase returns
* Batch and expiry operations
* Stock adjustments where granted
* Warehouse transfers
* Supplier view

Store Keeper must not by default:

* Post sales
* Receive customer payments
* Manage customer credit
* Manage organization settings
* View unrestricted financial reports

The permission matrix must mark every permission as:

```text
Allowed by default
Not allowed by default
Conditionally assignable
Platform only
```

---

# 31. Tenant, Branch, and Warehouse Enforcement

Define:

* Normal organization APIs derive organization context from authenticated membership.
* Organization users cannot choose another organization through request data.
* Platform APIs use an explicit platform authorization path.
* Every referenced branch must belong to the authenticated organization.
* Every referenced warehouse must belong to the authenticated organization.
* Assigned branch/warehouse access is validated before business execution.
* Record lookup by ID verifies organization ownership.
* Cross-tenant IDs produce a safe not-found or scope-denied response according to security policy.
* Imports, reports, exports, audit, and background operations remain tenant scoped.
* Platform access must not silently bypass normal tenant rules.

---

# 32. Subscription Plan Model

Plans remain:

```text
Starter
Business
Enterprise
```

Plan definitions are data driven.

A plan may configure:

* Monthly price
* Annual price
* Annual discount
* Trial eligibility
* Branch limit
* Warehouse limit
* Active-user limit
* Product limit
* Customer limit
* Supplier limit
* Import entitlement
* Report/export entitlement
* Audit-history entitlement
* Backup policy reference
* Dedicated-cloud eligibility
* Support-level reference

Exact commercial prices and numeric plan limits remain unresolved until approved by the product owner.

Do not hardcode them into application logic.

---

# 33. Subscription States

Define:

```text
pending_approval
trial
active
grace
suspended
cancelled
retained
deleted
```

Allowed lifecycle:

```text
pending_approval
→ trial or active
→ grace
→ suspended
→ active after approval/reactivation

active or trial
→ cancelled
→ retained
→ deleted through authorized process
```

Rules:

* State transitions require authorization.
* Every state transition is audited.
* Cancellation and deletion are separate.
* Suspension does not delete existing data.
* Reactivation preserves organization data.
* `deleted` requires an authorized retention/deletion process.

---

# 34. Trial and Grace Defaults

Release 1 defaults:

```text
Trial duration: 14 calendar days
Grace duration: 7 calendar days
Retention after cancellation: 90 calendar days
```

## Trial

* No payment method required.
* Available once per organization unless Super Admin approves an exception.
* Uses approved trial entitlements.
* Shows expiry warnings.
* Automatically enters grace when trial expires without approved activation.

## Grace

During grace:

* Existing plan entitlements remain operational.
* Persistent expiry and billing warnings are shown.
* Payment evidence may be submitted.
* No data is deleted.

After grace expiry:

* Subscription becomes suspended.

## Suspended

Suspended organizations may:

* Sign in
* View subscription status
* Submit payment evidence
* View or export historical data where policy allows
* Contact support through configured channels

Suspended organizations may not:

* Post sales
* Post purchases
* Post payments
* Adjust or transfer stock
* Create operational transactions
* Create new master data beyond billing/support requirements

---

# 35. Manual Billing Workflow

Supported evidence methods:

* Bank transfer
* JazzCash
* Easypaisa

Billing record statuses:

```text
draft
submitted
under_review
approved
rejected
expired
cancelled
```

Workflow:

```text
Owner submits payment evidence
→ billing record submitted
→ Super Admin reviews
→ approve or reject
→ approved record activates or extends subscription
→ audit event created
```

Rules:

* Standard organization users cannot approve billing evidence.
* Approval requires actor and timestamp.
* Rejection requires reason.
* Evidence access is permission restricted.
* Sensitive evidence must not appear in normal logs.
* Duplicate payment reference produces a warning and review requirement.
* Approval must be idempotent.
* One billing record must not extend a subscription twice.
* Automated payment gateways are excluded from Release 1.

---

# 36. Upgrade, Downgrade, and Renewal

## Upgrade

* Approved upgrade may become effective immediately.
* New entitlements become available after successful activation.
* Release 1 does not require automated proration.
* Manual commercial adjustment may be recorded in billing notes and audit.

## Downgrade

* Downgrade becomes effective at the next approved billing-period boundary.
* Existing data is not deleted.
* Resources above the new limit remain readable.
* New creation is blocked until usage is within limits or the plan changes.
* Downgrade must warn about limit conflicts before approval.

## Renewal

* Approved billing extends the subscription end date once.
* Duplicate approval must not extend twice.
* Annual and monthly periods use calendar-based subscription dates.

---

# 37. Entitlement and Limit Enforcement

Enforcement occurs on the backend.

Rules:

* Entitlements are evaluated from subscription and plan data.
* Frontend hiding is not enforcement.
* Soft warning occurs before applicable hard limit where configured.
* Hard limit blocks only new creation.
* Existing records remain accessible.
* Limits must not delete data.
* An update that does not increase limited usage may remain allowed.
* Dedicated cloud is an Enterprise entitlement.
* Shared SaaS remains the default.
* Dedicated and shared deployments use the same application codebase.

Every API endpoint must document whether it requires:

* Active subscription
* Trial/grace access
* Specific entitlement
* Numeric limit check
* No subscription check because it is platform or billing access

---

# 38. Security Logging and Sensitive Data

Do not log:

* Passwords
* Session tokens
* Password-reset tokens
* CSRF secrets
* Database connection strings
* Full payment evidence
* Unnecessary identity documents
* Full request bodies for sensitive endpoints

Logs may contain, where safe:

* Request ID
* Organization ID
* Actor ID
* Module
* Operation
* Safe error code
* Outcome
* Duration
* Transaction ID

Audit records and technical logs remain separate.

---

# 39. Backup and Restore Authorization

Define:

* Standard organization users cannot restore production data.
* Backup status is visible only to authorized platform operators.
* Restore execution requires `operations.restore.execute`.
* Restore requires controlled operational procedure.
* Restore requires verification before normal operations resume.
* Emergency direct database repair is not a normal business workflow.
* Emergency repair requires incident authorization, recovery plan, incident record, and reconciliation.
* Business transactions must not be edited directly through operational database access.

---

# 40. Security Testing Expectations

Define future tests for:

* Password hashing
* Login throttling
* Session expiration
* Session invalidation
* Cookie attributes
* CSRF rejection
* CORS allowlist
* Permission enforcement
* Tenant isolation
* Cross-tenant indirect-reference rejection
* Branch scope
* Warehouse scope
* Subscription entitlement
* Plan limits
* Business approval separation
* Reset-token single use
* Billing approval idempotency
* Sensitive logging restrictions
* Restore authorization
* Role default bundles
* Platform versus organization separation

P1-05 does not create tests.

---

# 41. Controlled Unresolved Details

Keep these unresolved:

* Exact framework and package versions
* Package manager
* Monorepo tool
* Exact commercial plan prices
* Exact numeric plan limits
* Final support channels
* Backup provider
* Hosting provider
* Dedicated-cloud topology
* Monitoring provider
* Exact log-retention period
* Exact idempotency-record retention period
* Exact authentication rate-limit numbers
* Exact import upload size
* Exact export generation threshold
* Tax and regulatory policy
* Exact report column definitions
* Exact import spreadsheet columns

Assign each unresolved item to P1-06, P1-07, implementation, commercial approval, or deployment tasks.

Do not invent values.

---

# 42. Traceability

Each new document must include traceability.

## Data Model

Map:

* Collection
* Owning module
* PRD prefixes
* BR prefixes
* Authoritative or projected status

## API Design

Map:

* Endpoint group
* Owning module
* Permission
* Subscription requirement
* Transaction owner

## Security and Authorization

Map:

* Security control
* Relevant NFR
* Enforced layer
* Required test category

## Subscription and Billing

Map:

* Subscription behaviour
* PRD requirement
* BR-SUB rule
* Data record
* API operation

Do not duplicate complete frozen documents.

---

# 43. README Update

Update README:

```text
Current task: P1-05
Status: Data, API, security, authorization, and subscription design drafted for review
Next task: P1-06 — Implementation roadmap and delivery plan
```

State:

* P1-01 through P1-04 are complete.
* P1-05 remains draft.
* Application implementation has not started.
* No application folders or packages have been initialized.

---

# 44. Project Index Update

Move these to Existing Documents:

* `DATA_MODEL.md`
* `API_DESIGN.md`
* `SECURITY_AUTHORIZATION.md`
* `SUBSCRIPTION_AND_BILLING.md`
* `tasks/P1-05.md`

Keep uncreated documents non-clickable.

Do not create P1-06.

---

# 45. Out of Scope

Do not:

* Modify frozen P1-02, P1-03, or P1-04 documents
* Modify `PROJECT_DECISIONS.md`
* Create Mongoose schemas
* Create collection code
* Create Express routes
* Create controllers
* Create services
* Create repositories
* Create Angular files
* Create package manifests
* Install packages
* Select framework versions
* Create CI/CD
* Create infrastructure
* Create tests
* Create API contract TypeScript files
* Create database migrations
* Begin P1-06

---

# 46. Acceptance Criteria

P1-05 is complete for review only when:

* All four documents exist.
* Every document remains draft.
* Collection ownership matches frozen module boundaries.
* Every tenant-owned collection requires organization scope.
* Platform-owned data is explicitly separated.
* MongoDB identifiers and API identifiers are defined.
* Money and quantity representations are defined.
* UTC timestamp and business-date handling are defined.
* Embedded and referenced-data rules are defined.
* Authoritative movements and projections are distinguished.
* Posted transaction immutability is preserved.
* Signed effect design is explicit.
* Canonical collections are documented.
* Required indexes are documented.
* Transaction boundaries are documented.
* Concurrency controls are documented.
* Idempotency rules are documented.
* API versioning and envelopes are documented.
* Error codes are documented.
* Pagination and filtering are documented.
* Master-data and transaction mutation patterns are documented.
* Endpoint inventory covers all canonical modules.
* Authentication uses secure opaque server sessions.
* Session defaults are documented.
* Password and reset policy are documented.
* CSRF, CORS, headers, and rate limiting are documented.
* Permission catalog is complete.
* Default role bundles are documented.
* Tenant, branch, and warehouse scope are explicit.
* Subscription states are explicit.
* Trial, grace, suspension, and retention are explicit.
* Manual billing workflow is explicit.
* Upgrade, downgrade, and renewal rules are explicit.
* Backend entitlement enforcement is explicit.
* Security logging restrictions are explicit.
* Backup/restore authorization is explicit.
* Security test expectations are documented.
* Traceability exists.
* Frozen documents remain unchanged.
* No implementation files are created.
* P1-06 is not started.

---

# 47. Required Completion Report

Return only:

## Summary

Summarize the technical design created.

## Files Created

List created files.

## Files Updated

List supporting files updated.

## Data Model

Report:

* Canonical collection count
* Tenant-owned collection count
* Platform-owned collection count
* Collections missing ownership
* Authoritative versus projected-state coverage
* Index coverage
* Transaction and concurrency coverage

## API Design

Report:

* Endpoint inventory count
* Module coverage
* Permission mapping coverage
* Subscription mapping coverage
* Idempotent endpoint count
* Missing endpoint groups
* Error-code coverage

## Security and Authorization

Report:

* Authentication strategy
* Session defaults
* Permission count
* Role-matrix coverage
* Tenant/branch/warehouse enforcement coverage
* Security-control coverage
* Missing controls

## Subscription and Billing

Report:

* State-machine coverage
* Trial/grace/retention defaults
* Manual billing workflow coverage
* Limit-enforcement coverage
* Upgrade/downgrade coverage
* Commercial unresolved items

## Traceability

Report:

* PRD mapping
* BR mapping
* Module ownership mapping
* Missing traceability

## Validation

Report:

* Duplicate collection-name check
* Duplicate permission check
* Endpoint-path duplicate check
* Missing organization-scope check
* Broken-link check
* Frozen-document modification check
* Unexpected-file check
* Unrelated-change check

## Remaining Risks or Blockers

Report genuine conflicts only.

## Confirmations

Confirm:

* `PROJECT_DECISIONS.md` was not modified
* Frozen P1-02 documents were not modified
* Frozen P1-03 documents were not modified
* Frozen P1-04 documents were not modified
* P1-06 was not started
* No application, schema, API implementation, framework, package, test, CI, or infrastructure files were created
* P1-05 remains draft

---

# Final P1-05 Technical-Integrity Cleanup

This section records the final review-pass cleanup specification applied to P1-05 draft documents before freeze.

Do not create a separate correction task.

After this cleanup passes validation, P1-05 shall be frozen without another broad documentation review.

## Files to Patch

Patch only:

* `docs/DATA_MODEL.md`
* `docs/API_DESIGN.md`
* `docs/SECURITY_AUTHORIZATION.md`
* `docs/SUBSCRIPTION_AND_BILLING.md`
* `docs/PROJECT_INDEX.md`
* `README.md`, only if status wording requires correction
* `docs/tasks/P1-05.md`, by appending this cleanup specification

Do not modify any frozen P1-02, P1-03, or P1-04 document.

The full cleanup requirements (sections 1–20) are applied in the P1-05 design documents. Summary of mandatory changes:

1. Organization onboarding and account activation (`account_activation_tokens`, public request + activate APIs)
2. Explicit session context (`activeContextType`, `activeMembershipId`, `activeOrganizationId`, context switch API)
3. CSRF clarification (`none-public` CSRF; CSRF on login/logout/activation/reset/onboarding/mutations; Origin/Referer)
4. Idempotency ownership moved to API Infrastructure — Transactions and Idempotency
5. Date-only `YYYY-MM-DD` strings; IANA organization timezone; BSON UTC timestamps
6. MongoDB transaction-capable replica-set/sharded topology prerequisite
7. Owner-presence invariant; conditional permission grant semantics on memberships
8. Dedicated opening-balance permissions
9. Account inflow/outflow and transfer/transaction reverse APIs
10. Source-specific reversal APIs; remove public generic corrective-transactions endpoint
11. Return endpoint integrity (sales-linked and without-invoice drafts)
12. One current subscription per organization; versioned immutable plan definitions
13. Explicit subscription lifecycle action endpoints
14. Billing record snapshots, single-apply approval, evidence storage refs, calendar period rules
15. Import and billing evidence opaque storage references
16. Additional required indexes
17. Minimal public health/liveness
18. Remove stale `tasks/P1-02R.md` link from project index
19. Expected counts after cleanup: collections 44, permissions 81, endpoints 157, idempotent 38
20. Validation checklist before freeze


