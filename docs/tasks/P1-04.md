# P1-04 — Architecture and Repository Structure

## Task Status

* Status: Complete
* Result: Architecture, module boundaries, and repository structure approved and frozen
* Completion date: 2026-08-04
* Architecture version: 1.0
* Canonical modules: 20
* Repository root: Agrivio/
* Next task: P1-05 — Data, API, Security, and Subscription Design

## Goal

Define the production-grade architecture, module boundaries, dependency rules, runtime flows, and target monorepo structure for Agrivio Release 1.

This task converts the frozen product requirements and business rules into an implementation architecture that later agents must follow.

This task must create:

* `docs/ARCHITECTURE.md`
* `docs/MODULE_BOUNDARIES.md`
* `docs/REPOSITORY_STRUCTURE.md`

This task must update:

* `docs/PROJECT_INDEX.md`
* `README.md`

This task defines the target architecture only.

Do not initialize Angular, Express, MongoDB, TypeScript workspaces, packages, source folders, CI, infrastructure, or application code.

---

# 1. Files to Read

Read only:

* `AGENTS.md`
* `README.md`
* `docs/PROJECT_INDEX.md`
* `docs/PROJECT_DECISIONS.md`
* `docs/PRD.md`
* `docs/RELEASE_1_SCOPE.md`
* `docs/BUSINESS_RULES.md`
* `docs/DOMAIN_GLOSSARY.md`
* `docs/tasks/P1-04.md`

Do not scan unrelated files.

---

# 2. Source-of-Truth Hierarchy

Use this authority order:

1. `docs/PROJECT_DECISIONS.md`
2. `docs/PRD.md`
3. `docs/RELEASE_1_SCOPE.md`
4. `docs/BUSINESS_RULES.md`
5. `docs/DOMAIN_GLOSSARY.md`
6. `docs/tasks/P1-04.md`
7. `AGENTS.md`

The P1-02 and P1-03 documents are frozen.

P1-04 may define architecture that supports those documents, but it must not:

* Add product scope
* Remove frozen requirements
* Change business formulas
* Rename approved domain concepts
* Reopen business decisions
* Introduce client-specific code forks
* Turn the system into microservices
* Begin implementation

If an architectural requirement conflicts with a frozen document, report the conflict as a blocker. Do not silently change the frozen source.

---

# 3. Confirmed Architecture Baseline

The architecture must preserve these finalized decisions:

* Cloud-first web application
* Angular frontend
* Node.js backend
* Express backend framework
* TypeScript across frontend and backend
* MongoDB persistence
* Monorepo
* Modular monolith
* Multi-tenant architecture from day one
* Shared SaaS as the default deployment
* Provider-managed dedicated cloud for eligible Enterprise customers
* Same application codebase for shared SaaS and dedicated cloud
* Backend permission-based authorization
* Backend-enforced organization, branch, and warehouse boundaries
* Thin controllers
* Business logic in services
* Repository abstraction for reusable or complex persistence access
* Mongoose as the MongoDB persistence layer
* Feature-based frontend
* No client-specific code forks
* No microservices in Release 1
* No offline synchronization in Release 1

The required backend request path is:

```text
Route
→ authentication and permission enforcement
→ tenant, branch, and warehouse scope enforcement
→ request validation
→ controller
→ application service
→ repository or module interface
→ Mongoose persistence
```

Controllers must not contain domain calculations or persistence logic.

---

# 4. Deliverables

## 4.1 `docs/ARCHITECTURE.md`

Define:

* Architecture goals
* System context
* Container-level architecture
* Modular-monolith structure
* Frontend architecture
* Backend architecture
* Request lifecycle
* Transaction lifecycle
* Tenant isolation
* Authorization architecture
* Module communication
* Atomic workflow architecture
* Error handling
* Observability
* Configuration and secrets
* Reporting and alerts
* Import architecture
* Deployment model
* Testing architecture
* Architecture guardrails
* Controlled unresolved architecture details

## 4.2 `docs/MODULE_BOUNDARIES.md`

Define:

* Canonical module catalog
* Responsibility of each module
* Data ownership
* Public module interfaces
* Allowed dependencies
* Forbidden dependencies
* Cross-module transaction rules
* Cross-module query rules
* Module dependency graph
* Cyclic-dependency prevention
* Reporting exceptions
* Import orchestration rules
* Audit ownership
* Architecture enforcement expectations

## 4.3 `docs/REPOSITORY_STRUCTURE.md`

Define:

* Target monorepo layout
* Frontend folder structure
* Backend folder structure
* Module folder template
* Shared-package rules
* Test layout
* Documentation layout
* Script and tooling locations
* Naming conventions
* Import-boundary conventions
* Public module surfaces
* File-ownership rules
* Prohibited folders and dumping grounds

Do not create the documented folders in P1-04.

---

# 5. Document Control

Each new document must contain:

```text
Document status: Draft for P1-04 review
Current version: 0.1
Approval status: Not yet frozen
```

Use the actual update date.

State clearly:

* Frozen PRD defines what Release 1 must provide.
* Frozen Business Rules define business behaviour and formulas.
* Architecture documents define how the system will be structured to support those requirements.
* Repository Structure defines the target layout for later initialization.
* P1-04 does not create implementation.

Do not freeze the documents automatically.

---

# 6. Architecture Goals

`ARCHITECTURE.md` must define these goals:

* Strict tenant isolation
* Correct atomic financial and stock workflows
* Explicit module ownership
* No circular business-module dependencies
* No client-specific code forks
* Same codebase for shared and dedicated deployments
* Maintainable modular-monolith boundaries
* Testable business logic
* Traceable stock and financial changes
* Consistent authorization enforcement
* Scalable organization, branch, warehouse, product, and transaction volumes
* Clear separation between frontend, backend, persistence, and infrastructure
* Ability to evolve modules without a broad rewrite
* Clear migration path if a module later needs extraction
* No premature distributed-system complexity

Do not claim that Release 1 uses microservices, event sourcing, CQRS, distributed transactions, or multiple databases per business module.

---

# 7. System Context

Define these actors and systems:

## Actors

* Super Admin
* Owner
* Manager
* Cashier
* Store Keeper

## System

* Agrivio web application
* Agrivio API
* MongoDB database
* Provider-managed production operations

## External boundaries

Release 1 may interact operationally with:

* Browser printing through OS-configured printers
* Manual billing evidence for bank transfer, JazzCash, and Easypaisa
* Backup storage selected in a later deployment task
* Error-monitoring provider selected later

Do not invent:

* Payment gateway
* SMS provider
* WhatsApp integration
* Email automation
* Cloud provider
* Queue provider
* Cache provider
* Object-storage provider
* Accounting integration

Include a Mermaid system-context diagram.

---

# 8. Container Architecture

Define these logical containers:

## Browser client

Responsibilities:

* Render Angular web interface
* Capture user intent
* Perform client-side form validation for usability
* Show loading, empty, error, permission, and validation states
* Send authenticated API requests
* Show only permitted navigation and controls
* Use browser printing

The browser must not be treated as the authorization boundary.

## API application

Responsibilities:

* Authentication
* Permission enforcement
* Tenant isolation
* Branch and warehouse scope enforcement
* Request validation
* Business-rule execution
* Atomic transaction orchestration
* Persistence access
* Audit creation
* Report generation
* Import orchestration
* In-app alert data
* Operational logging

## MongoDB

Responsibilities:

* Persistent operational data
* Transaction snapshots
* Stock movements
* Ledger effects
* Account movements
* Audit events
* Subscription state
* Import and correction history

Do not define schemas or collection names in P1-04.

## Operational environment

Responsibilities:

* Configuration
* Secrets
* Backups
* Restore operations
* Health monitoring
* Error monitoring
* Deployment

Exact hosting topology remains unresolved.

Include a Mermaid container diagram.

---

# 9. Backend Layering

The backend must be a modular monolith.

Each business module must use these logical layers:

## Routes

Responsibilities:

* Register HTTP entry points
* Attach authentication
* Attach permission checks
* Attach scope checks
* Attach validation
* Forward to controllers

Routes must not contain business logic.

## Validation

Responsibilities:

* Validate request shape
* Validate required fields
* Validate basic formats
* Reject malformed input before controller execution

Validation must not replace domain rules in services.

## Controllers

Responsibilities:

* Translate validated requests into service calls
* Extract authenticated context
* Return success or error responses
* Remain thin

Controllers must not:

* Query Mongoose directly
* Calculate stock
* Calculate ledgers
* Calculate weighted-average cost
* Post transactions
* Decide permissions
* Orchestrate multi-module business workflows

## Application services

Responsibilities:

* Execute use cases
* Apply business rules
* Coordinate module interfaces
* Define transaction boundaries
* Enforce business invariants
* Produce auditable outcomes

Application services are the primary business-workflow owners.

## Repositories

Responsibilities:

* Encapsulate persistence access
* Enforce tenant-scoped access
* Support transaction/session context
* Map persisted records to module-level data structures
* Expose business-oriented persistence operations

A module must not import another module’s repository.

## Mongoose persistence

Responsibilities:

* MongoDB persistence mapping
* Index definitions
* Persistence validation
* Query implementation

Exact schemas and indexes belong in P1-05.

Include the backend request-flow diagram:

```text
HTTP request
→ authentication
→ permission enforcement
→ organization scope
→ branch/warehouse scope
→ request validation
→ controller
→ application service
→ module interfaces/repositories
→ MongoDB transaction
→ audit and response
```

---

# 10. Frontend Architecture

The Angular frontend must use feature-based organization.

Define these top-level frontend areas:

## App shell

Responsibilities:

* Application bootstrap
* Main routing
* Layout composition
* Global loading behaviour
* Global error presentation

## Core

May contain only application-wide singleton infrastructure such as:

* Authentication session
* HTTP client configuration
* Error handling
* Route guards
* Permission-context access
* Application configuration
* Layout services
* Logging integration

Core must not contain business-feature components.

## Features

Each product area owns its:

* Pages
* Feature components
* Feature routes
* Forms
* Feature services
* Data-access layer
* Feature models
* Feature-specific validation
* Feature tests

Feature modules must not import internal files from another feature.

Cross-feature interaction must use:

* Published feature facade
* Approved shared service
* Router navigation
* Stable API contract

## Shared

May contain only genuinely reusable:

* UI components
* Form controls
* Pipes
* Directives
* Formatting utilities
* Accessibility helpers
* Generic layout primitives

Shared must not contain:

* Sales rules
* Inventory rules
* Customer rules
* Permission decisions
* API orchestration for a specific feature
* Generic `helpers` containing unrelated code

## State management

Use local feature state by default.

Do not require a global state-management library in P1-04.

A later implementation task may introduce shared state only where a documented cross-feature need exists.

Frontend permission checks improve usability only. The backend remains authoritative.

Include a frontend dependency diagram.

---

# 11. Canonical Backend Modules

`MODULE_BOUNDARIES.md` must define these canonical modules.

The exact internal file count is not defined in P1-04.

## 11.1 Platform

Owns:

* Super Admin platform operations
* Organization approval orchestration
* Platform-level operational views
* Platform scope boundaries

Must not own organization business data.

## 11.2 Identity and Access

Owns:

* Authentication identity
* Sessions
* Password reset
* Permission evaluation
* User authentication context

Does not own organization subscription entitlements.

## 11.3 Organizations

Owns:

* Organization lifecycle
* Organization profile
* Owner-presence invariant
* Organization-level settings references

Does not own employee authentication credentials.

## 11.4 Subscriptions

Owns:

* Plans
* Subscription state
* Trial
* Grace
* Suspension
* Reactivation
* Entitlements
* Plan-limit evaluation
* Manual billing verification status

## 11.5 Locations

Owns:

* Branches
* Warehouses
* Employee branch assignments
* Employee warehouse assignments
* Branch invoice-prefix configuration

Does not own stock quantities.

## 11.6 Catalog and Pricing

Owns:

* Product categories
* Products
* Base units
* Packaging units
* Conversion configuration
* Tracking modes
* Price tiers
* Product prices

Does not own posted transaction snapshots or current stock.

## 11.7 Customers

Owns:

* Customer records
* Customer type
* Credit policy
* Credit-limit configuration
* Customer opening receivable/advance source request

Does not own posted sales invoices.

## 11.8 Suppliers

Owns:

* Supplier records
* Supplier opening payable/advance source request
* Supplier reference information

Does not own purchases.

## 11.9 Inventory

Owns:

* Stock movements
* Warehouse stock
* Batch stock
* Expiry facts
* FEFO/FIFO allocation
* Weighted-average cost
* Stock valuation
* Stock adjustments
* Warehouse transfers

Inventory balances are movement-derived.

## 11.10 Purchases

Owns:

* Purchase drafts
* Posted purchases
* Supplier-reference duplicate checks
* Purchase posting orchestration
* Purchase cancellation orchestration
* Purchase-return source validation

Purchases must use Inventory, Payments, Accounts, and Audit through public interfaces.

## 11.11 Sales

Owns:

* Sale drafts
* Posted invoices
* Invoice sequencing
* Sale posting orchestration
* Credit-sale validation orchestration
* Sale cancellation orchestration
* Posted sale-price and cost snapshots

Sales must use Inventory, Customers, Payments, Accounts, and Audit through public interfaces.

## 11.12 Payments and Ledgers

Owns:

* Customer payments
* Supplier payments
* Payment allocations
* Customer advances
* Supplier advances
* Receivable effects
* Payable effects
* Customer ledger
* Supplier ledger
* Payment correction

Must not own account balances.

## 11.13 Accounts and Expenses

Owns:

* Cash accounts
* Bank accounts
* JazzCash accounts
* Easypaisa accounts
* Account movements
* Account transfers
* Opening account balances
* Expense categories
* Expenses
* Expense correction

Account balances are movement-derived.

## 11.14 Returns and Corrections

Owns:

* Sales-return orchestration
* Purchase-return orchestration
* Return-without-invoice approval flow
* Sellable/unsellable classification
* Refund or ledger-adjustment orchestration
* Shared correction workflow conventions

It must use the source Sale or Purchase module rather than recreating source transaction logic.

## 11.15 Alerts

Owns:

* Low-stock alert queries
* Expiry alert queries
* Expired-stock alert queries
* Dead-stock alert queries
* Customer-due alert queries
* Supplier-due alert queries
* In-app notification presentation data

Alerts do not own authoritative stock or ledger balances.

## 11.16 Reporting

Owns:

* Dashboard query composition
* Fixed report query composition
* Report filters
* Report exports
* Reconciliation-oriented report views

Reporting is read-only.

It must not post stock, payment, ledger, account, or correction effects.

## 11.17 Imports

Owns:

* Import job lifecycle
* Template version recognition
* Preview validation
* Row and field error reporting
* Import orchestration
* Import-result audit references

Imports must invoke target-module application interfaces.

Imports must not directly write another module’s collections or repositories.

## 11.18 Audit

Owns:

* Audit-event persistence
* Audit query access
* Actor, reason, approval, and source references

Audit does not replace technical logs.

Business modules must create audit events through the Audit public interface.

## 11.19 Settings

Owns:

* Organization-configurable operational settings not owned by a specialized module
* References to configured policies

A setting already owned by a domain module remains with that module.

For example:

* Credit policy belongs to Customers
* Expiry threshold belongs to Inventory or Alerts according to the approved ownership table
* Subscription settings belong to Subscriptions

## 11.20 Operations

Owns technical operational concerns:

* Health checks
* Backup-status visibility
* Restore-operation coordination
* Operational readiness
* Error-monitoring integration
* Structured logging integration

Operations does not own business audit records.

---

# 12. Module Public Interfaces

Every business module must expose a deliberate public surface.

Other modules may use only:

* Public application interface
* Public query interface
* Published domain type or contract
* Approved after-commit event

Other modules must not import:

* Internal controller
* Internal service implementation
* Internal repository
* Mongoose model
* Persistence mapper
* Internal validator
* Private utility
* Internal folder path

The public interface may later be represented through an explicit module entry point.

Exact TypeScript interface definitions belong in P1-05 or implementation tasks.

---

# 13. Mandatory Dependency Rules

Define an explicit allowed dependency table.

At minimum preserve these directions:

```text
Platform
→ Organizations
→ Subscriptions
→ Identity and Access

Sales
→ Customers
→ Catalog and Pricing
→ Inventory
→ Payments and Ledgers
→ Accounts and Expenses
→ Audit

Purchases
→ Suppliers
→ Catalog and Pricing
→ Inventory
→ Payments and Ledgers
→ Accounts and Expenses
→ Audit

Returns and Corrections
→ Sales
→ Purchases
→ Inventory
→ Payments and Ledgers
→ Accounts and Expenses
→ Audit

Payments and Ledgers
→ Customers
→ Suppliers
→ Accounts and Expenses
→ Audit

Inventory
→ Catalog and Pricing
→ Locations
→ Audit

Alerts
→ read-only Inventory queries
→ read-only Payments and Ledgers queries

Reporting
→ approved read-only query interfaces

Imports
→ public application interfaces of target modules

Operations
→ infrastructure and operational status interfaces
```

The final document must refine this into a clear matrix.

---

# 14. Forbidden Dependencies

The following are prohibited:

* Circular module dependencies
* Controller-to-controller calls
* Repository-to-controller calls
* Repository-to-service calls outside its module
* Direct Mongoose access from controllers
* Direct Mongoose access from frontend code
* One module importing another module’s Mongoose model
* One module importing another module’s repository
* One feature importing another feature’s internal frontend files
* Audit module depending on every business module
* Reporting module mutating business data
* Alerts module becoming source of truth for stock or ledgers
* Imports writing directly to another module’s persistence layer
* Shared packages containing product-specific business rules
* Generic `common`, `utils`, or `helpers` folders becoming unowned dumping grounds
* Business modules depending on HTTP request or response objects
* Domain calculations inside route middleware
* Client-specific branches in business logic
* Dedicated-cloud-only product behaviour
* Cross-tenant queries without explicit platform authorization

Document how architecture tests will later enforce these rules.

---

# 15. Cross-Module Transactions

Business-critical workflows must execute synchronously inside one database transaction where required by frozen business rules.

Examples:

## Sale posting

The Sales application service owns orchestration of:

* Invoice
* Invoice sequence
* Inventory allocation
* Stock movements
* COGS snapshot
* Receivable effect
* Payment allocation
* Account movement
* Audit event

## Purchase posting

The Purchases application service owns orchestration of:

* Purchase
* Inventory receipt
* Batch stock
* Weighted-average-cost update
* Payable effect
* Payment allocation
* Account movement
* Audit event

## Return posting

Returns and Corrections owns orchestration across:

* Source transaction validation
* Return
* Stock and batch effect
* Cost or valuation effect
* Ledger effect
* Refund or account effect
* Audit event

## Warehouse transfer

Inventory owns the atomic outbound and inbound stock effects.

Rules:

* The use-case-owning service controls the transaction boundary.
* Participating module interfaces must accept the shared transaction context.
* Participating modules must not commit independently.
* A failed workflow must roll back all authoritative effects.
* Audit events required by the transaction must participate in the same transaction.
* After-commit actions must not determine authoritative stock or financial state.

Do not define Mongoose session APIs in this task.

---

# 16. Events and Asynchronous Behaviour

Release 1 remains a synchronous modular monolith for authoritative workflows.

In-process events may later be used for non-authoritative after-commit behaviour such as:

* Refreshing alert projections
* Invalidating report caches if introduced later
* Operational notifications
* Non-critical analytics

Events must not be the only mechanism responsible for:

* Stock movement
* Weighted-average cost
* Receivable
* Payable
* Payment allocation
* Account balance
* Invoice sequence
* Return posting
* Cancellation
* Reversal
* Audit events required for business correctness

Do not introduce:

* Message broker
* Distributed event bus
* Event sourcing
* Saga orchestration
* Distributed transactions

Any future extraction of a module requires a separate architecture decision.

---

# 17. Tenant Isolation Architecture

Define these mandatory rules:

* Every tenant-owned record belongs to exactly one organization.
* Authenticated organization context is resolved before business execution.
* Every tenant-owned repository operation requires organization scope.
* Organization scope must not be an optional repository filter.
* Record lookup by ID must also verify organization ownership.
* Branch and warehouse references must belong to the same organization.
* Cross-organization relations are prohibited unless explicitly platform-owned.
* Super Admin platform operations must be explicitly separated from organization operations.
* Platform authorization must not silently bypass organization rules.
* Background or scheduled operations must iterate organizations explicitly.
* Reports and exports must remain tenant-scoped.
* Imports must remain tenant-scoped.
* Audit queries must remain tenant-scoped except explicitly authorized platform operations.
* Cache keys, if introduced later, must include organization scope.
* File exports, if introduced later, must include ownership and access controls.

Architecture testing must later verify:

* Repository methods require organization context
* Cross-tenant access is rejected
* IDs from another organization cannot be used through indirect references
* Platform operations are explicitly separated

Do not define schemas in P1-04.

---

# 18. Authorization Architecture

Define:

* Authentication identifies the user.
* Permission evaluation decides whether an operation is allowed.
* Organization context limits tenant ownership.
* Branch scope limits branch operations.
* Warehouse scope limits warehouse operations.
* Subscription entitlement controls plan capability and limits.
* Business approval rules are separate from permissions.

These must remain distinct:

```text
Authentication
≠ permission
≠ tenant scope
≠ branch or warehouse assignment
≠ subscription entitlement
≠ Manager or Owner business approval
```

Required order:

```text
Authenticate
→ resolve platform or organization context
→ check subscription access where applicable
→ check permission
→ check branch/warehouse scope
→ validate request
→ apply business rules
```

Frontend guards may mirror permissions for usability but are never authoritative.

The exact permission matrix belongs in P1-05 or its designated authorization task.

---

# 19. Reporting Architecture

Reporting must use authoritative transactional data.

Define:

* Dashboard and reports are read-only.
* Reporting calculations must reuse approved business-rule definitions.
* Gross-profit logic must not be independently reimplemented in multiple places.
* Stock reports derive from stock movements and maintained stock state.
* Ledger reports derive from signed ledger effects.
* Account reports derive from account movements.
* Cancelled and reversed transactions must not be double-counted.
* Report access remains organization, branch, warehouse, and permission scoped.
* Large report execution strategy remains unresolved until performance baselines.

Preferred architecture:

* Published module query interfaces for normal reads
* Dedicated read-only reporting queries where cross-module aggregation is necessary
* No mutation through reporting access
* No direct dependency from operational modules back to Reporting

Exact report queries, projections, and indexes belong in P1-05.

---

# 20. Alert Architecture

Alerts are read-only interpretations of authoritative business data.

Define:

* Alert configuration belongs to the appropriate owning module.
* Alert results derive from Inventory, Sales, and Ledger query interfaces.
* Alerts do not maintain independent conflicting stock or balance totals.
* Alerts are shown through authenticated dashboard and in-app notification center.
* Alert calculation may be on-demand or projection-based.
* The final calculation strategy remains an implementation decision.
* No SMS, WhatsApp, email automation, or browser push architecture is added.

---

# 21. Import Architecture

Define this flow:

```text
Upload or provide workbook
→ identify template version
→ parse safely
→ validate structure
→ validate every row
→ resolve target-module references
→ produce preview and errors
→ receive explicit confirmation
→ execute target-module operations atomically
→ create audit and import result
```

Rules:

* Import owns orchestration, not target business data.
* Target modules remain responsible for their own business rules.
* Imports must not bypass service-layer validation.
* Imports must not directly call another module’s Mongoose model.
* Imports must not silently overwrite records.
* Import execution must be logically all-or-nothing according to frozen business rules.
* Exact spreadsheet templates belong in a later task.

---

# 22. Error Handling Architecture

Define a centralized error-handling strategy.

Error categories must include:

* Authentication error
* Authorization error
* Tenant-scope violation
* Branch/warehouse-scope violation
* Validation error
* Business-rule violation
* Conflict
* Not found
* Transaction failure
* External operational failure
* Unexpected internal failure

Rules:

* Internal stack traces must not be exposed to normal users.
* Validation and business errors must be distinguishable.
* Errors must carry correlation context for technical logs.
* Sensitive data must not be included in errors or logs.
* Controllers pass errors to centralized handling.
* Business services use domain-appropriate errors rather than HTTP-specific response creation.

Exact API error response shape belongs in P1-05.

---

# 23. Observability Architecture

Define:

* Structured application logs
* Correlation or request identifier
* Organization identifier where safe and appropriate
* Actor identifier where safe and appropriate
* Module and operation name
* Error category
* Transaction outcome
* Health checks
* Dependency health
* Backup-failure visibility
* Production error capture
* Audit records separate from technical logs

Do not log:

* Passwords
* Session secrets
* Reset tokens
* Full payment evidence containing sensitive data
* Unnecessary personal data
* Database connection secrets

Exact monitoring provider remains unresolved.

---

# 24. Configuration and Secrets

Define:

* Environment-specific configuration
* No secrets in source control
* No secrets in frontend bundles
* Startup validation for required configuration
* Shared SaaS and dedicated cloud use the same codebase
* Deployment differences are configuration-driven
* Client-specific feature forks are prohibited
* Plan entitlements are data/configuration driven, not code forks
* Production, staging, test, and local settings remain separated

Exact environment variable names belong in P1-07 or deployment tasks.

---

# 25. Deployment Architecture

Document two supported deployment modes.

## Shared SaaS

* Provider-managed
* Multiple organizations
* Shared application codebase
* Strict tenant isolation
* Shared platform operations
* Deployment topology selected later

## Dedicated cloud

* Provider-managed
* Eligible Enterprise customer
* Same application codebase
* Dedicated environment configuration where contracted
* Dedicated database configuration where contracted
* Provider-controlled deployment and updates
* No client-managed fork
* No self-service provisioning

Do not select:

* Cloud provider
* Region
* Container platform
* CI/CD platform
* Database hosting provider
* Backup provider
* Monitoring provider

Include a Mermaid deployment-mode diagram.

---

# 26. Testing Architecture

Define these test levels:

## Unit tests

Cover:

* Pure calculations
* Business policies
* Value transformations
* Permission-policy evaluation
* FEFO/FIFO selection
* WAC
* Rounding
* Signed corrective effects

## Module integration tests

Cover:

* Service and repository behaviour
* Mongoose persistence
* Tenant scoping
* Transaction boundaries
* Index-dependent rules
* Module public interfaces

## Cross-module workflow tests

Cover:

* Sale posting
* Purchase posting
* Return posting
* Warehouse transfer
* Payments and allocations
* Cancellations and reversals
* Imports

## API contract tests

Cover later-approved API contracts.

## Frontend tests

Cover:

* Feature components
* Forms
* Permission-aware presentation
* Keyboard operation
* Error states
* Critical POS workflow

## End-to-end tests

Cover frozen Release 1 workflows.

## Architecture tests

Must later verify:

* No forbidden module imports
* No cross-module model imports
* No cross-module repository imports
* No controller persistence access
* No frontend feature-internal cross-imports
* Every tenant repository requires organization scope
* No circular dependencies

P1-04 defines test categories only. It does not create tests.

---

# 27. Target Monorepo Structure

`REPOSITORY_STRUCTURE.md` must define this target structure:

```text
Agrivio/
├── apps/
│   ├── web/
│   └── api/
├── packages/
│   ├── api-contracts/
│   ├── tooling-config/
│   └── test-support/
├── docs/
│   ├── tasks/
│   ├── PROJECT_INDEX.md
│   ├── PROJECT_DECISIONS.md
│   ├── PRD.md
│   ├── RELEASE_1_SCOPE.md
│   ├── BUSINESS_RULES.md
│   ├── DOMAIN_GLOSSARY.md
│   ├── ARCHITECTURE.md
│   ├── MODULE_BOUNDARIES.md
│   └── REPOSITORY_STRUCTURE.md
├── scripts/
├── tools/
├── AGENTS.md
├── README.md
└── .gitignore
```

These folders are targets for later initialization. Do not create them in P1-04.

## Package rules

### `api-contracts`

May later contain:

* Approved request and response contracts
* Shared API enums
* Stable transport-level types

Must not contain:

* Backend domain services
* Mongoose models
* Angular services
* Business calculations
* Repository interfaces tied to persistence

### `tooling-config`

May later contain shared:

* TypeScript configuration
* Lint configuration
* Formatting configuration
* Test configuration

### `test-support`

May later contain reusable:

* Test builders
* Fixtures
* Tenant-isolation helpers
* Transaction test utilities

Must not contain production business logic.

---

# 28. Backend Target Structure

Define the future API layout:

```text
apps/api/
├── src/
│   ├── bootstrap/
│   ├── config/
│   ├── infrastructure/
│   │   ├── database/
│   │   ├── logging/
│   │   ├── monitoring/
│   │   └── transactions/
│   ├── middleware/
│   ├── modules/
│   │   ├── platform/
│   │   ├── identity-access/
│   │   ├── organizations/
│   │   ├── subscriptions/
│   │   ├── locations/
│   │   ├── catalog/
│   │   ├── customers/
│   │   ├── suppliers/
│   │   ├── inventory/
│   │   ├── purchases/
│   │   ├── sales/
│   │   ├── payments-ledgers/
│   │   ├── accounts-expenses/
│   │   ├── returns-corrections/
│   │   ├── alerts/
│   │   ├── reporting/
│   │   ├── imports/
│   │   ├── audit/
│   │   ├── settings/
│   │   └── operations/
│   └── shared/
└── tests/
    ├── integration/
    ├── workflows/
    ├── architecture/
    └── e2e/
```

`shared/` must use an explicit allowlist.

Permitted examples:

* Domain-neutral error base
* Request context type
* Money/quantity primitives where approved
* Generic result type
* Date utility with no business policy

Prohibited examples:

* Sale calculation
* Stock allocation
* Customer credit policy
* WAC policy
* Subscription business logic
* Generic repositories for unrelated modules

---

# 29. Backend Module Template

Each module should follow a consistent future template:

```text
modules/<module>/
├── public/
├── routes/
├── validation/
├── controllers/
├── services/
├── repositories/
├── persistence/
├── policies/
├── mappers/
├── types/
└── tests/
```

Responsibilities:

* `public/`: module interfaces available to other modules
* `routes/`: HTTP registration
* `validation/`: request-shape validation
* `controllers/`: HTTP adaptation
* `services/`: use cases and orchestration
* `repositories/`: persistence abstractions and module-owned queries
* `persistence/`: Mongoose models and implementations
* `policies/`: module-owned business policies
* `mappers/`: persistence/transport mapping
* `types/`: module-internal types
* `tests/`: module unit and integration tests

Not every module must contain every folder.

Empty folders must not be created merely to match a template.

---

# 30. Frontend Target Structure

Define:

```text
apps/web/
├── src/
│   ├── app/
│   │   ├── core/
│   │   ├── layout/
│   │   ├── shared/
│   │   └── features/
│   │       ├── public/
│   │       ├── authentication/
│   │       ├── platform/
│   │       ├── organization/
│   │       ├── subscriptions/
│   │       ├── users-access/
│   │       ├── branches-warehouses/
│   │       ├── catalog/
│   │       ├── customers/
│   │       ├── suppliers/
│   │       ├── inventory/
│   │       ├── purchases/
│   │       ├── sales-pos/
│   │       ├── payments-ledgers/
│   │       ├── returns/
│   │       ├── accounts-expenses/
│   │       ├── alerts/
│   │       ├── dashboard/
│   │       ├── reports/
│   │       ├── imports/
│   │       ├── audit/
│   │       └── settings/
│   └── assets/
└── tests/
    └── e2e/
```

Feature template:

```text
features/<feature>/
├── pages/
├── components/
├── data-access/
├── services/
├── models/
├── routes/
└── tests/
```

Not every feature must contain every folder.

No feature may import another feature’s internal folder.

---

# 31. Naming Conventions

Define:

* Directories: `kebab-case`
* TypeScript files: `kebab-case`
* Classes and interfaces: `PascalCase`
* Variables and functions: `camelCase`
* Constants: use project-approved consistent convention
* Test files: consistent `.spec.ts` or later-approved project convention
* Business module names: canonical names from `MODULE_BOUNDARIES.md`
* No abbreviations that obscure domain meaning
* Use `organization`, not interchangeable `tenant`, inside business-module naming unless referring specifically to tenancy infrastructure
* Use `warehouse`, not generic `store`
* Use `customer advance`, not generic `credit balance`
* Use `weighted-average cost`, not ambiguous `average price`

Exact lint rules belong in P1-07.

---

# 32. Public Import Rules

Define future import expectations:

* Import from another module only through its public surface.
* Internal relative imports remain inside the same module.
* Frontend features import shared UI only through approved shared exports.
* Infrastructure may depend on technical configuration but must not own business policy.
* Business services must not depend on Express request/response types.
* Persistence implementations may depend on Mongoose.
* Domain policies must not depend on Mongoose.
* API contracts must not import backend persistence types.
* Test-support packages must not become production dependencies.

---

# 33. Prohibited Repository Patterns

Explicitly prohibit:

```text
src/common/
src/helpers/
src/utils/
src/services/
src/models/
src/controllers/
```

as root-level dumping grounds for unrelated business features.

Also prohibit:

* One global models directory containing every business model
* One global service directory containing every business service
* One global controller directory
* One global repository with access to every collection
* Generic CRUD service used as a substitute for domain services
* Shared business logic without a clear owning module
* Frontend `shared` containing feature pages
* Client-specific directories
* Separate source trees for shared SaaS and dedicated cloud

Technical shared folders are allowed only with narrow documented ownership.

---

# 34. Architecture Decision Records

P1-04 must state when a future ADR is required.

ADR required for:

* Change from modular monolith to another architecture
* New deployment model
* New database technology
* Queue or message broker introduction
* Cache introduction that affects consistency
* Background worker introduction
* Global frontend state library
* Cross-module data ownership change
* Module extraction
* New external integration
* Authentication strategy change
* Multi-currency architecture
* Offline synchronization
* Client-specific fork proposal

Do not create ADR files in P1-04 unless an unresolved conflict requires one.

---

# 35. Controlled Unresolved Architecture Details

Record these unresolved details:

* Exact Node.js version
* Exact Angular version
* Exact TypeScript version
* Exact Express version
* Exact Mongoose version
* Package manager
* Monorepo orchestration tool
* Build tool configuration
* Test frameworks
* API versioning format
* API response envelope
* Authentication token/session implementation
* Exact permission matrix
* MongoDB transaction configuration
* Database naming
* Collection names
* Index definitions
* Reporting query strategy
* Alert projection strategy
* Import parser library
* File upload limits
* Cache requirement
* Background worker requirement
* Hosting provider
* Dedicated-cloud topology
* CI/CD provider
* Backup provider
* Monitoring provider
* Log-retention policy
* Performance thresholds

Assign each unresolved detail to P1-05, P1-06, P1-07, or a later deployment task.

Do not invent them.

---

# 36. Traceability

`ARCHITECTURE.md` must include a traceability table:

| Frozen requirement area | Architecture section | Owning module |
| ----------------------- | -------------------- | ------------- |

Cover:

* Tenancy
* Authorization
* Products and pricing
* Inventory
* Purchases
* Sales
* Payments and ledgers
* Accounts
* Returns
* Warehouse transfers
* Alerts
* Reporting
* Imports
* Audit
* Subscriptions
* Backup and restore

`MODULE_BOUNDARIES.md` must link module responsibilities to relevant PRD and BR prefixes.

Do not copy all requirements into the architecture documents.

---

# 37. README Update

Update README to state:

```text
Current task: P1-04
Status: Architecture, module boundaries, and repository structure drafted for review
Next task: P1-05 — Data, API, security, and subscription design
```

State:

* P1-01 through P1-03 are complete.
* P1-04 remains under review.
* Application implementation has not started.
* No source folders or packages have been initialized.

---

# 38. Project Index Update

Move these to Existing Documents:

* `ARCHITECTURE.md`
* `MODULE_BOUNDARIES.md`
* `REPOSITORY_STRUCTURE.md`
* `tasks/P1-04.md`

Keep uncreated documents non-clickable.

Do not create P1-05 documents.

---

# 39. Out of Scope

Do not:

* Modify frozen P1-02 documents
* Modify frozen P1-03 documents
* Modify `PROJECT_DECISIONS.md`
* Create Angular application
* Create Express application
* Create TypeScript configuration
* Install packages
* Create `apps/`
* Create `packages/`
* Create source folders
* Create schemas
* Create collection names
* Define API endpoints
* Define request or response bodies
* Define permission matrix
* Define exact environment variables
* Create CI/CD
* Select hosting provider
* Select package manager
* Select monorepo tool
* Select framework versions
* Create Docker files
* Create deployment manifests
* Create tests
* Begin P1-05

---

# 40. Acceptance Criteria

P1-04 is complete for review only when:

* `docs/ARCHITECTURE.md` exists.
* `docs/MODULE_BOUNDARIES.md` exists.
* `docs/REPOSITORY_STRUCTURE.md` exists.
* Architecture remains a modular monolith.
* Shared SaaS and dedicated cloud use the same codebase.
* System-context diagram exists.
* Container diagram exists.
* Backend request-flow diagram exists.
* Frontend dependency diagram exists.
* Deployment-mode diagram exists.
* Backend layers are explicit.
* Controllers remain thin.
* Business logic belongs in services.
* Repositories own persistence access.
* Mongoose access is module-owned.
* Frontend is feature-based.
* Frontend authorization is not authoritative.
* Canonical module catalog is complete.
* Every module has explicit responsibility.
* Every module has explicit data ownership.
* Public module interfaces are required.
* Allowed dependencies are documented.
* Forbidden dependencies are documented.
* Circular dependencies are prohibited.
* Cross-module transaction ownership is defined.
* Critical workflows remain atomic.
* Events are not used as authoritative transaction state.
* Tenant isolation architecture is explicit.
* Branch and warehouse scope enforcement is explicit.
* Authorization, entitlement, scope, and approval are distinguished.
* Reporting remains read-only.
* Alerts remain derived from authoritative data.
* Imports invoke target-module services.
* Error architecture is defined.
* Observability architecture is defined.
* Configuration and secrets boundaries are defined.
* Shared and dedicated deployment boundaries are defined.
* Testing architecture is defined.
* Target monorepo structure is documented.
* Backend module template is documented.
* Frontend feature structure is documented.
* Shared-folder restrictions are explicit.
* Root dumping grounds are prohibited.
* Architecture ADR triggers are defined.
* Controlled unresolved details are documented.
* Traceability to frozen documents exists.
* README and PROJECT_INDEX are updated.
* Frozen files remain unchanged.
* No source folders, packages, schemas, APIs, or framework files are created.
* P1-04 remains draft pending human review.

---

# 41. Required Completion Report

Return only:

## Summary

Summarize the architecture documentation created.

## Files Created

List all created files.

## Files Updated

List supporting files updated.

## Architecture Diagrams

List each diagram created.

## Module Catalog

Report:

* Number of canonical modules
* Module names
* Modules missing ownership, if any

## Dependency Validation

Report:

* Allowed dependency table result
* Forbidden dependency coverage
* Circular-dependency review
* Public-interface rule coverage
* Cross-module repository/model import rule coverage

## Architecture Coverage

Confirm coverage of:

* Tenant isolation
* Permission enforcement
* Atomic transactions
* Frontend boundaries
* Backend layering
* Reporting
* Alerts
* Imports
* Audit
* Deployment
* Testing

## Repository Structure Validation

Report:

* Target frontend structure documented
* Target backend structure documented
* Shared-package boundaries documented
* Naming conventions documented
* Dumping-ground folders prohibited

## Traceability

Report:

* Frozen PRD areas mapped
* Business-rule prefixes mapped
* Missing traceability, if any

## Controlled Unresolved Details

List unresolved architecture details retained.

## Validation

Report:

* Broken-link check
* Duplicate module-name check
* Frozen-document modification check
* Unexpected file-creation check
* Unrelated-change check

## Remaining Risks or Blockers

Report genuine architectural conflicts only.

## Confirmations

Confirm:

* `PROJECT_DECISIONS.md` was not modified
* `PRD.md` was not modified
* `RELEASE_1_SCOPE.md` was not modified
* `BUSINESS_RULES.md` was not modified
* `DOMAIN_GLOSSARY.md` was not modified
* P1-05 was not started
* No application, framework, schema, API, package, or infrastructure files were created
* P1-04 remains draft
