# P1-07 — Toolchain and Repository Initialization Specification

## Task Status

* Status: Complete
* Result: Toolchain and repository initialization specification approved and frozen
* Completion date: 2026-08-04
* Toolchain version: 1.0.1
* Next work item: R1-F00-001 — Monorepo workspace bootstrap
* Errata: Executable-bootstrap correction applied on 2026-08-04; documents remain frozen at 1.0.1

## Goal

Finalize and freeze the exact Release 1 toolchain, framework versions, package-management strategy, Nx workspace design, initialization sequence, local MongoDB replica-set setup, testing stack, linting and formatting tools, CI foundation, development commands, and dependency-update policy for Agrivio.

P1-07 is the final documentation phase before implementation.

This task must create:

* `docs/TOOLCHAIN.md`
* `docs/REPOSITORY_INITIALIZATION.md`
* `docs/DEVELOPMENT_WORKFLOW.md`
* `docs/TEST_STRATEGY.md`

This task must update:

* `README.md`
* `docs/PROJECT_INDEX.md`
* `docs/tasks/P1-07.md`

P1-07 must not initialize the monorepo or create application files.

If all validation passes, freeze P1-07 in the same run. Do not require a separate freeze prompt.

---

# 1. Files to Read

Read only:

* `AGENTS.md`
* `README.md`
* `docs/PROJECT_INDEX.md`
* `docs/PROJECT_DECISIONS.md`
* `docs/PRD.md`
* `docs/RELEASE_1_SCOPE.md`
* `docs/BUSINESS_RULES.md`
* `docs/DOMAIN_GLOSSARY.md`
* `docs/ARCHITECTURE.md`
* `docs/MODULE_BOUNDARIES.md`
* `docs/REPOSITORY_STRUCTURE.md`
* `docs/DATA_MODEL.md`
* `docs/API_DESIGN.md`
* `docs/SECURITY_AUTHORIZATION.md`
* `docs/SUBSCRIPTION_AND_BILLING.md`
* `docs/IMPLEMENTATION_ROADMAP.md`
* `docs/DELIVERY_PLAN.md`
* `docs/QUALITY_GATES.md`
* `docs/tasks/P1-07.md`

Do not scan unrelated files.

The agent may run read-only commands such as:

```bash
git remote -v
node --version
npm view <package> version
npm view <package> versions --json
```

Do not install packages or modify package files.

---

# 2. Authority Order

Use:

1. `PROJECT_DECISIONS.md`
2. Frozen P1-02 requirements and scope
3. Frozen P1-03 business rules and glossary
4. Frozen P1-04 architecture and repository structure
5. Frozen P1-05 technical design
6. Frozen P1-06 implementation planning
7. `P1-07.md`
8. `AGENTS.md`

Do not modify any frozen document.

Report a genuine compatibility conflict instead of silently changing architecture or product scope.

---

# 3. Required Deliverables

## 3.1 `docs/TOOLCHAIN.md`

Define:

* Exact approved versions
* Version compatibility
* Package roles
* Module/runtime format
* Version-pinning rules
* Upgrade policy
* Prohibited alternatives
* Official verification evidence
* Controlled future upgrade process

## 3.2 `docs/REPOSITORY_INITIALIZATION.md`

Define:

* Preflight checks
* Exact initialization order
* Root files to create
* Nx workspace configuration
* Angular generation configuration
* Express API generation configuration
* Shared-package generation
* Local MongoDB replica-set initialization
* CI foundation sequence
* Bootstrap validation commands
* Expected file tree after F00
* Rollback procedure for failed initialization

## 3.3 `docs/DEVELOPMENT_WORKFLOW.md`

Define:

* Local prerequisites
* Installation process
* Root scripts
* Development commands
* Database commands
* Branch naming
* Commit naming
* Pull-request gates
* Dependency update workflow
* Environment-file rules
* Troubleshooting boundaries
* AI-agent workflow expectations

## 3.4 `docs/TEST_STRATEGY.md`

Define:

* Unit-test stack
* Angular component-test stack
* API integration-test stack
* MongoDB transaction-test environment
* Architecture-test approach
* E2E stack
* Test locations
* Naming conventions
* CI test layers
* Coverage policy
* Fixture boundaries
* Test-data isolation
* Required smoke tests for F00

---

# 4. Exact Approved Version Baseline

Use these exact versions:

| Tool                  | Approved version |
| --------------------- | ---------------- |
| Node.js               | `24.18.0`        |
| pnpm                  | `11.17.0`        |
| Nx                    | `23.1.0`         |
| `@nx/angular`         | `23.1.0`         |
| `@nx/node`            | `23.1.0`         |
| `@nx/js`              | `23.1.0`         |
| `@nx/eslint`          | `23.1.0`         |
| `@nx/vitest`          | `23.1.0`         |
| `@nx/playwright`      | `23.1.0`         |
| Angular core packages | `22.0.8`         |
| Angular CLI           | `22.0.8`         |
| TypeScript            | `6.0.3`          |
| Express               | `5.2.1`          |
| Mongoose              | `9.8.0`          |
| MongoDB Server        | `8.2.12`         |
| Vitest                | `4.1.10`         |
| Playwright Test       | `1.62.0`         |
| ESLint                | `10.7.0`         |
| Prettier              | `3.9.6`          |

Rules:

* Use stable releases only.
* Do not use `next`, `rc`, `beta`, `alpha`, canary, nightly, or preview packages.
* Angular framework, Angular build packages, and Angular CLI must use the same patch line.
* All installed `@nx/*` packages must exactly match the `nx` version.
* TypeScript must remain within Angular 22’s supported `6.0.x` range.
* Do not replace Node 24 LTS with Node 26 Current.
* Do not replace Express with NestJS.
* Do not replace MongoDB/Mongoose.
* Do not replace Nx with Turborepo, Lerna, or a custom task runner.
* Do not replace pnpm with npm, Yarn, or Bun.

For packages not listed above, such as compatible type packages, ESLint plugins, Angular ESLint packages, `jsdom`, or generator-installed support packages:

* Determine the exact stable compatible version.
* Record it in `TOOLCHAIN.md`.
* Do not install it during P1-07.
* Reject prerelease versions.
* Do not change the approved major toolchain to satisfy an incompatible plugin.

If a required package cannot support this baseline, report a blocker and do not freeze P1-07.

---

# 5. Package and Lockfile Policy

Use:

```json
{
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": ">=11.17.0 <12"
  }
}
```

Required files after F00:

```text
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
nx.json
.nvmrc
.node-version
.npmrc
```

Rules:

* `.nvmrc` and `.node-version` contain `24.18.0`.
* Set `save-exact=true` in `.npmrc`.
* Direct dependencies and dev dependencies use exact versions.
* Commit `pnpm-lock.yaml`.
* CI uses:

```bash
pnpm install --frozen-lockfile
```

* Do not commit `package-lock.json`, `yarn.lock`, or Bun lockfiles.
* Internal packages use the pnpm workspace protocol.
* Do not use `latest`, `*`, broad major ranges, or unbounded dependency ranges.
* Dependency updates occur through dedicated reviewed commits.
* Lockfile changes must not be mixed invisibly into unrelated feature work.

---

# 6. Monorepo Design

Use:

```text
Package manager: pnpm workspaces
Task orchestration: Nx integrated workspace
Application directory: apps/
Internal package directory: packages/
Package scope: @agrivio
```

Configure Nx workspace layout:

```json
{
  "workspaceLayout": {
    "appsDir": "apps",
    "libsDir": "packages"
  }
}
```

Initial projects:

```text
apps/web
apps/api
packages/api-contracts
packages/tooling-config
packages/test-support
```

Rules:

* Preserve the frozen `Agrivio/` repository root.
* Do not use `libs/`.
* Do not create empty folders for future modules.
* Create a module or feature directory only when its first real implementation, public interface, test, or configuration file exists.
* Do not use `.gitkeep` to pre-create all future modules.
* Nx project names and package names must be deterministic and documented.
* Nx Cloud is not enabled in Release 1 bootstrap.
* Use local Nx caching.
* Remote caching requires a later explicit approval.

---

# 7. Angular Application Specification

Generate `apps/web` as:

```text
Angular 22
Standalone components
Strict mode
SCSS
Angular routing
Zoneless change detection
Vitest Angular unit tests
No SSR
No SSG
No service worker
No PWA
No Module Federation
No global state-management library
```

Rules:

* Use Angular’s supported application build system.
* Use standalone APIs.
* Use strict template and TypeScript checks.
* Do not create business-feature folders as empty placeholders.
* The initial application contains only the minimum files required to build, serve, and run its scaffold test.
* Do not create Release 1 pages or business UI in F00.
* Do not select a third-party UI component framework in P1-07.
* Preserve SCSS as the styling language.
* Public landing-page implementation belongs to its roadmap work item, not the scaffold.

---

# 8. Express API Specification

Generate `apps/api` as:

```text
Node.js 24
Express 5
TypeScript
Native ESM
Strict TypeScript
Nx Node application
Vitest
No business routes
No business schemas
No authentication implementation
No module placeholders
```

Required module format:

```json
{
  "type": "module"
}
```

API compiler baseline:

```text
target: ES2024
module: NodeNext
moduleResolution: NodeNext
strict: true
```

Rules:

* The scaffold may expose only the minimum bootstrap behaviour required for a smoke test.
* The public health endpoint belongs to F01 unless the generator requires a minimal temporary smoke route.
* Temporary smoke behaviour must not be mistaken for the final health contract.
* Do not create business controllers, services, repositories, or persistence models.
* Do not use `ts-node` as the production runtime.
* Do not add NestJS.
* Do not add a generic CRUD framework.
* Do not add a generic global repository.

---

# 9. TypeScript Policy

Shared strict settings must include:

```text
strict
noUncheckedIndexedAccess
exactOptionalPropertyTypes
noImplicitOverride
noImplicitReturns
noFallthroughCasesInSwitch
useUnknownInCatchVariables
forceConsistentCasingInFileNames
```

Rules:

* Avoid `any`; exceptional usage requires a narrow documented reason.
* API contracts must not import Express, Angular, Mongoose, or backend persistence types.
* Frontend must not import API implementation code.
* Backend domain/application logic must not depend on Express request/response objects.
* Mongoose types remain inside persistence boundaries.
* Internal package references use workspace packages or approved Nx path configuration.
* Do not use path aliases to bypass public module boundaries.

Document whether `skipLibCheck` is enabled and why.

---

# 10. Linting and Formatting

Use:

```text
ESLint flat configuration
typescript-eslint
Angular ESLint
Nx module-boundary enforcement
Prettier
```

Responsibilities:

* ESLint detects correctness, architecture, and code-quality problems.
* Prettier controls formatting.
* Do not duplicate formatting rules in ESLint.

Formatting baseline:

```text
UTF-8
LF line endings
Single quotes where supported
Trailing commas where supported
100-character print width
Two-space indentation
Final newline required
```

Required checks:

```bash
pnpm format
pnpm format:check
pnpm lint
```

Architecture linting must later support:

* Forbidden cross-module imports
* Frontend feature-internal import restrictions
* Public module entry-point enforcement
* No cross-module Mongoose-model imports
* No cross-module repository imports

Custom business architecture assertions may be implemented during F00/F01 where ordinary lint rules are insufficient.

---

# 11. Testing Stack

Use:

| Test category           | Tool                                                                         |
| ----------------------- | ---------------------------------------------------------------------------- |
| Angular unit/component  | Angular native Vitest integration                                            |
| API unit                | Vitest                                                                       |
| API module integration  | Vitest against real local MongoDB replica set                                |
| Transaction/concurrency | Vitest against real MongoDB replica set                                      |
| Architecture tests      | Nx boundaries plus custom Vitest/Node assertions                             |
| End-to-end              | Playwright Test                                                              |
| Browser coverage        | Chromium required in normal CI; Firefox and WebKit in release/nightly matrix |
| Coverage engine         | Vitest V8 coverage                                                           |

Do not use:

* Karma
* Jasmine as the primary runner
* Jest
* Cypress
* Protractor
* `mongodb-memory-server` as the authoritative transaction test environment
* Mocked MongoDB for transaction correctness

Mocks may be used for pure unit tests, but transaction, index, tenant-isolation, concurrency, and repository tests must use a real replica-set-capable MongoDB environment.

---

# 12. Test Locations

Use frozen repository boundaries.

Backend:

```text
apps/api/src/modules/<module>/tests/
apps/api/tests/integration/
apps/api/tests/workflows/
apps/api/tests/architecture/
apps/api/tests/e2e/
```

Frontend:

```text
apps/web/src/app/features/<feature>/tests/
apps/web/tests/e2e/
```

Shared reusable test support:

```text
packages/test-support/
```

Rules:

* Test support must not contain production business logic.
* Unit tests may be colocated where framework conventions require.
* Integration and workflow tests remain clearly separated.
* E2E tests must interact through public UI/API surfaces.
* Architecture tests must not be hidden inside ordinary unit-test suites.
* Every test creates isolated organization and actor context.
* Tests must not depend on execution order.

Naming:

```text
*.spec.ts
*.integration.spec.ts
*.workflow.spec.ts
*.architecture.spec.ts
*.e2e.spec.ts
```

---

# 13. Coverage Policy

Do not invent a misleading global coverage threshold for an empty F00 scaffold.

Use these principles:

* New business calculations require complete branch coverage for documented rule cases.
* Transaction workflows require success, rollback, retry, and duplicate-request coverage.
* Tenant and permission enforcement require explicit allow and deny cases.
* Coverage must not be used as a substitute for reconciliation or security tests.
* A global numeric baseline must be established no later than the F01 exit review once meaningful production code exists.
* Coverage reduction requires explicit review.
* Generated files, configuration-only files, and type-only contracts may be excluded with documented rules.

---

# 14. Local MongoDB Replica Set

Use:

```text
MongoDB 8.2.12
Single-node local replica set
Replica-set name: rs0
Docker Compose v2
Development database: agrivio_dev
Test database prefix: agrivio_test_
```

Planned location:

```text
tools/docker/mongodb/compose.yml
scripts/mongodb/
```

Required commands:

```text
pnpm db:up
pnpm db:init
pnpm db:status
pnpm db:logs
pnpm db:down
pnpm db:reset
```

Rules:

* Bind the development port to localhost only.
* Local and CI MongoDB must support multi-document transactions.
* Health checking must wait until a primary is elected.
* Integration tests use isolated database names.
* Tests clean their own data.
* Production must not use the local Docker configuration.
* Production authentication, TLS, backup, and topology remain deployment concerns.
* A standalone MongoDB server is not an accepted development substitute.
* `db:reset` must be clearly destructive and limited to local data.

---

# 15. Environment and Secrets

Planned files:

```text
.env.example
.env.local
.env.test
```

Rules:

* Commit `.env.example`.
* Ignore `.env.local` and secret-bearing variants.
* Do not place secrets in Angular environment output.
* Browser configuration contains public values only.
* API validates required environment values at startup.
* Test configuration must not use production credentials.
* Never print complete environment configuration.
* Local MongoDB credentials or connection details must not be reused in production.

P1-07 documents variable categories but does not finalize every application environment-variable name.

---

# 16. Root Command Contract

Document these root commands for F00:

```text
pnpm dev
pnpm dev:web
pnpm dev:api

pnpm build
pnpm build:web
pnpm build:api

pnpm lint
pnpm typecheck
pnpm test
pnpm test:unit
pnpm test:integration
pnpm test:architecture
pnpm e2e

pnpm format
pnpm format:check

pnpm check
pnpm affected:check

pnpm db:up
pnpm db:init
pnpm db:status
pnpm db:logs
pnpm db:down
pnpm db:reset
```

`pnpm check` must run, in deterministic order:

```text
format check
→ lint
→ type check
→ unit tests
→ architecture tests
→ build
```

Transaction integration and E2E suites may remain separate due to environment startup cost, but CI must run them in their designated jobs.

Do not rely on globally installed Nx, Angular CLI, TypeScript, or pnpm executables.

---

# 17. CI Foundation

Select:

```text
GitHub Actions
```

Use:

```text
Ubuntu runner
Node.js 24.18.0
pnpm 11.17.0
Frozen lockfile installation
Nx local cache
MongoDB 8.2.12 replica-set service through Docker Compose
Playwright browser installation through the official Playwright command
```

Required planned workflows:

## Quality

Runs:

* Install
* Format check
* Lint
* Type check
* Unit tests
* Architecture tests
* Build

## Integration

Runs:

* Start MongoDB replica set
* Initialize replica set
* API integration tests
* Transaction smoke test
* Tenant-isolation smoke test

## E2E smoke

Runs:

* Start MongoDB
* Start API
* Start web application
* Run Chromium Playwright smoke test

Rules:

* CI uses exact Node and pnpm versions.
* CI never modifies the lockfile.
* CI must fail on an uncommitted generated diff.
* Production secrets are not used in pull-request workflows.
* Deployment pipelines are outside F00.
* Nx Cloud is disabled initially.
* GitHub-hosted dependency update automation may be considered after F00, but it must not automatically merge major updates.

---

# 18. Development Workflow

Branch names:

```text
task/R1-F00-001
task/R1-F01-001
fix/<short-description>
docs/<short-description>
```

Commit format:

```text
<type>: <imperative summary>
```

Approved types:

```text
build
chore
ci
docs
feat
fix
perf
refactor
revert
test
```

Rules:

* One roadmap work item per primary branch.
* Do not combine unrelated work items.
* Every pull request references:

  * Roadmap work-item ID
  * Frozen source documents
  * Tests run
  * Quality gates
  * Remaining risks
* No direct frozen-document edits from an implementation branch.
* Generated changes must be reviewed rather than blindly accepted.
* AI agents must read the specific roadmap work item and relevant frozen sources before editing.
* Agents must not scan the full repository when the work item gives an explicit file scope.
* Agents return the completion report required by the active work item.

---

# 19. Dependency Upgrade Policy

## Patch updates

May be proposed through a dedicated dependency PR with:

* Changelog review
* Lockfile diff
* Full applicable quality checks

## Minor updates

Require:

* Compatibility review
* Migration-note review
* Full quality checks

## Major updates

Require:

* ADR or formal technical decision
* Compatibility matrix
* Migration plan
* Full regression
* Rollback plan

Additional rules:

* Angular major updates occur one supported major at a time.
* Nx migrations must be reviewed before execution.
* Node major changes require CI and production-runtime compatibility validation.
* MongoDB upgrades require feature-compatibility and backup/restore planning.
* TypeScript must remain inside Angular’s supported range.
* Do not automatically update framework majors.
* Security patches may be expedited but still require validation.

---

# 20. Initialization Sequence

`REPOSITORY_INITIALIZATION.md` must define this exact F00 order:

1. Verify P1-07 freeze tag and clean working tree.
2. Verify Node.js and pnpm versions.
3. Create root package metadata and version files.
4. Create `pnpm-workspace.yaml`.
5. Install exact Nx core and plugins.
6. Initialize Nx in the existing documentation repository.
7. Set `appsDir=apps` and `libsDir=packages`.
8. Generate `apps/web` with approved Angular options.
9. Generate `apps/api` with approved Express/Node options.
10. Create the three approved non-empty shared packages.
11. Configure root TypeScript, ESLint, and Prettier.
12. Add root command contract.
13. Add local MongoDB replica-set configuration.
14. Add F00 smoke and architecture tests.
15. Add GitHub Actions quality, integration, and E2E foundations.
16. Run all F00 gates.
17. Verify no business feature implementation exists.
18. Record F00 completion evidence.

Do not use a new nested workspace directory.

The existing repository root remains:

```text
Agrivio/
```

---

# 21. Expected F00 Repository Tree

Document the expected minimum tree after F00 without creating it during P1-07:

```text
Agrivio/
├── apps/
│   ├── web/
│   └── api/
├── packages/
│   ├── api-contracts/
│   ├── tooling-config/
│   └── test-support/
├── tools/
│   └── docker/
│       └── mongodb/
├── scripts/
│   └── mongodb/
├── docs/
├── .github/
│   └── workflows/
├── package.json
├── pnpm-lock.yaml
├── pnpm-workspace.yaml
├── nx.json
├── tsconfig.base.json
├── eslint.config.mjs
├── .prettierrc
├── .prettierignore
├── .editorconfig
├── .npmrc
├── .nvmrc
├── .node-version
├── .env.example
├── AGENTS.md
└── README.md
```

No future domain-module or frontend-feature placeholder trees are created in F00.

---

# 22. F00 Bootstrap Acceptance Gates

The initialization specification must prove that F00 will require:

* Exact Node version check
* Exact pnpm version check
* Frozen lockfile
* Workspace project graph
* Empty web application build
* Empty API application build
* Web scaffold test
* API boot smoke test
* Type checking
* ESLint
* Formatting check
* Architecture-boundary fixture
* MongoDB replica-set primary election
* Multi-document transaction commit test
* Multi-document transaction rollback test
* CI quality job
* CI integration job
* CI E2E smoke job
* No business routes
* No business schemas
* No business feature folders
* No client-specific code
* No production deployment configuration

---

# 23. Prohibited Decisions and Tools

Do not select or introduce:

* NestJS
* Next.js
* React
* Vue
* npm as package manager
* Yarn
* Bun
* Turborepo
* Lerna
* Jest
* Karma
* Cypress
* Protractor
* Native mobile tooling
* Electron
* Module Federation
* Microservices
* Nx Cloud
* Redis
* Queue or broker
* Production Docker images
* Kubernetes
* Hosting provider
* Backup provider
* Monitoring provider
* UI component framework
* Global frontend state library
* Tax libraries
* Accounting framework

These require a later approved need or architecture decision.

---

# 24. Validation

Before freezing P1-07, verify:

1. Every core tool has one exact stable version.
2. No prerelease dependency is selected.
3. Angular, Node.js, and TypeScript compatibility is valid.
4. All Nx plugin versions match Nx.
5. Angular package versions match Angular CLI.
6. Package manager and lockfile strategy are deterministic.
7. Existing repository initialization does not create a nested workspace.
8. Frozen `apps/` and `packages/` paths are preserved.
9. No `libs/` directory is planned.
10. No future empty module or feature folders are planned.
11. Angular uses strict standalone SCSS configuration.
12. Express API uses strict TypeScript and ESM.
13. MongoDB development topology supports transactions.
14. Integration tests do not rely on mocked MongoDB for transaction correctness.
15. Vitest and Playwright responsibilities are distinct.
16. Root command contract is complete.
17. CI provider and jobs are explicit.
18. CI uses frozen lockfile installation.
19. Version update policy is explicit.
20. No business implementation is created.
21. No package file, application folder, source file, CI workflow, or Docker file is created during P1-07.
22. Frozen P1-02 through P1-06 documents remain unchanged.
23. All document links resolve.
24. F00 has an exact executable initialization order.
25. No genuine blocker remains.

---

# 25. Freeze in the Same Run

If all validation passes, set these documents to:

```text
Document status: Frozen for Release 1
Current version: 1.0.1
Approval status: Approved for repository initialization
Last updated: 2026-08-04
```

Documents:

* `docs/TOOLCHAIN.md`
* `docs/REPOSITORY_INITIALIZATION.md`
* `docs/DEVELOPMENT_WORKFLOW.md`
* `docs/TEST_STRATEGY.md`

Mark `docs/tasks/P1-07.md` complete:

```text
Status: Complete
Result: Toolchain and repository initialization specification approved and frozen
Completion date: 2026-08-04
Toolchain version: 1.0.1
Next work item: R1-F00-001 — Monorepo workspace bootstrap
```

Update README and Project Index:

* P1-01 through P1-07 are complete.
* P1-07 documents are frozen at version 1.0.1.
* Documentation planning is complete.
* Application implementation has not started.
* Next work item is `R1-F00-001`.
* F00 may now begin.

If any validation fails:

* Keep P1-07 draft.
* Do not partially freeze.
* Report only genuine blockers.

---

# 26. Out of Scope

Do not:

* Modify frozen documents
* Create `package.json`
* Create a lockfile
* Install packages
* Initialize Nx
* Generate Angular or Express applications
* Create `apps/`
* Create `packages/`
* Create source files
* Create MongoDB Compose files
* Create GitHub Actions workflows
* Create tests
* Begin F00
* Begin any business feature
* Create another planning task

---

# 27. Required Completion Report

Return only:

## Summary

## Files Created

## Files Updated

## Exact Version Matrix

Report every selected direct tool and version.

## Compatibility Validation

Report:

* Angular/Node compatibility
* Angular/TypeScript compatibility
* Nx/Angular compatibility
* Nx plugin-version alignment
* Test-runner compatibility

## Monorepo Specification

Report:

* Package manager
* Workspace manager
* Application directory
* Package directory
* Package scope
* Empty-placeholder policy
* Nested-workspace result

## Application Scaffold Specification

Report:

* Angular options
* Express/Node options
* TypeScript module format
* Business-code exclusion result

## Testing Specification

Report:

* Unit runner
* Integration environment
* Architecture testing
* E2E runner
* MongoDB transaction-test topology

## CI Specification

Report:

* Provider
* Required jobs
* Frozen-lockfile policy
* MongoDB service strategy
* Browser strategy

## Command Contract

Report command count and missing commands.

## Validation

Report:

* Prerelease dependency check
* Duplicate tool-selection check
* Version mismatch check
* Broken-link check
* Frozen-document modification check
* Unexpected-file check
* F00-start check
* Unrelated-change check

## Freeze Result

Confirm whether P1-07 was frozen at version 1.0.

## Remaining Genuine Blockers

## Confirmations

Confirm:

* No frozen document was modified
* No dependency was installed
* No package or lockfile was created
* No application or source folder was created
* No CI, Docker, test, or infrastructure file was created
* F00 was not started
