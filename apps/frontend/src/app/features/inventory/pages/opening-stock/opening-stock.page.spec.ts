import { Component } from '@angular/core';
import { OpeningStockPage } from './opening-stock.page';

describe('OpeningStockPage', () => {
  it('is defined', () => {
    expect(OpeningStockPage).toBeTruthy();
  });
});
