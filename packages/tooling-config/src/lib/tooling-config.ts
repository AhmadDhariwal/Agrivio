/**
 * Package marker for `@agrivio/tooling-config`.
 * Shared configuration files are consumed via package exports
 * (`typescript/*`, `prettier`, `eslint/base`, `vitest/node`).
 */
export const TOOLING_CONFIG_PACKAGE = '@agrivio/tooling-config' as const;
