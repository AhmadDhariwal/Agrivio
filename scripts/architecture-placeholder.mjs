/**
 * Architecture-boundary suite placeholder until R1-F00-008.
 * Keeps `npm run test:architecture` and `npm run check` runnable in F00 Batch A.
 */
console.log('[agrivio] Architecture-boundary tests placeholder — harness lands in R1-F00-008.');
process.exit(0);
